# Landing product: The delivered public market index levels fund performance is compared against, one row per index and date.
#
# The estate's `landed` label gives Batch Ingestion to the ingestion
# runtime and this relation, its validation, its publication and its retry
# wrapper to the SQL translator, so the relation is materialised from the
# delivered extract with no column changed (architecture section 5, the
# landing profile; owner ruling R1).
#
# Its schema has one origin: the relation it lands, bound here with the
# fields that relation delivers (owner ruling R5). Every column is a
# string, because a delivered extract states its values as text; the lane
# products downstream declare the types they need and cast to them. This
# is the only product that reads the delivered relation: nothing else in
# the estate does.
product:
  name: benchmark_index_returns
  domain: reference
  version: "1.0"
  layer: landed
  owner: reference-domain

sources:
  - contract: reference.benchmark_index_returns_delivery@1
    kind: fixture
    fixture:
      relation: benchmark_index_returns
      fields:
        - {name: benchmark_id, type: string}
        - {name: benchmark_name, type: string}
        - {name: index_date, type: string}
        - {name: index_value, type: string}
        - {name: currency, type: string}
        - {name: source_system, type: string}
    expect:
      fields: [benchmark_id, benchmark_name, index_date]

steps:
  - pattern: batch_ingestion
    scope: full
    cadence: daily
    extract_mode: full
  - pattern: data_validation
    on_failure: abort
    rules:
      - {field: benchmark_id, not_null: true}
      - {field: index_date, not_null: true}
      - {field: index_value, not_null: true}
  - pattern: data_contracts
  - pattern: lineage_capture
  - pattern: metadata_capture
    description: "The delivered public market index levels fund performance is compared against, one row per index and date."
    ownership: reference-domain
  - pattern: schema_publish
  - pattern: data_publish
    publication_mode: atomic

target:
  shape: declared
  contract:
    freshness: "daily by 05:00 UTC"
    access:
      classification: internal

checkpointing:
  granularity: step
  max_retries: 3
  backoff: exponential
