# Landing product: The VANTORA book-of-record general partner feed as it was delivered, one row per delivered manager record.
#
# The estate's `landed` label gives Batch Ingestion to the ingestion
# runtime and this relation, its validation, its publication and its retry
# wrapper to the SQL translator, so the relation is materialised from the
# delivered extract with no column changed (architecture section 5, the
# landing profile; owner ruling R1).
#
# Its schema has one origin: the relation it lands, bound here with the
# fields that relation delivers (owner ruling R5). Every column is a
# string, because a delivered extract states its values as text; the lane
# products downstream declare the types they need and cast to them. This
# is the only product that reads the delivered relation: nothing else in
# the estate does.
product:
  name: vantora_gp_extract
  domain: investment
  version: "1.0"
  layer: landed
  owner: investment-domain

sources:
  - contract: investment.vantora_gp_extract_delivery@1
    kind: fixture
    fixture:
      relation: raw_vantora_gps
      fields:
        - {name: vantora_gp_id, type: string}
        - {name: gp_name, type: string}
        - {name: gp_lei, type: string}
        - {name: shared_external_id, type: string}
        - {name: domicile, type: string}
        - {name: relationship_start_date, type: string}
    expect:
      fields: [vantora_gp_id, gp_name, gp_lei]

steps:
  - pattern: batch_ingestion
    scope: full
    cadence: daily
    extract_mode: full
  - pattern: data_validation
    on_failure: abort
    rules:
      - {field: vantora_gp_id, not_null: true}
      - {field: vantora_gp_id, unique: true}
      - {field: gp_name, not_null: true}
  - pattern: data_contracts
  - pattern: lineage_capture
  - pattern: metadata_capture
    description: "The VANTORA book-of-record general partner feed as it was delivered, one row per delivered manager record."
    ownership: investment-domain
  - pattern: schema_publish
  - pattern: data_publish
    publication_mode: atomic

target:
  shape: declared
  contract:
    freshness: "daily by 05:00 UTC"
    access:
      classification: confidential

checkpointing:
  granularity: step
  max_retries: 3
  backoff: exponential
