-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(
    materialized='incremental',
    incremental_strategy=dpf_incremental_strategy({"bigquery": "merge", "duckdb": "delete+insert"}),
    unique_key=["decision_id"]
) }}

with
checked_input as (
    select
        decision_id,
        external_deal_id,
        decision,
        conditions,
        actor,
        decided_at
    from {{ ref("investment__deal_decision_log__checked") }}
)

select
    decision_id,
    external_deal_id,
    decision,
    conditions,
    actor,
    decided_at
from checked_input
