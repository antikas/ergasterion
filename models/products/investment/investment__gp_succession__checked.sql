-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        source_record_id,
        source_predecessor_gp_id,
        source_successor_gp_id,
        source_event_type,
        source_effective_on,
        succession_source_system
    from {{ ref("investment__vantora_gp_succession") }}
),
step_00_batch_transfer as (
    select
        source_record_id,
        source_predecessor_gp_id,
        source_successor_gp_id,
        source_event_type,
        source_effective_on,
        succession_source_system
    from source_00
)

select
    source_record_id,
    source_predecessor_gp_id,
    source_successor_gp_id,
    source_event_type,
    source_effective_on,
    succession_source_system,
    coalesce(source_record_id is null, true) as violation_00,
    coalesce(source_effective_on is null, true) as violation_01
from step_00_batch_transfer
