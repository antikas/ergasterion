-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        asset_uid,
        vehicle_code,
        issuer_name_text,
        issuer_lei,
        vendor_company_id,
        industry_group,
        industry_detail,
        country_iso2,
        holding_status,
        first_reported_on
    from {{ ref("raw_chrono_portfolio_companies") }}
)

select
    asset_uid,
    vehicle_code,
    issuer_name_text,
    issuer_lei,
    vendor_company_id,
    industry_group,
    industry_detail,
    country_iso2,
    holding_status,
    first_reported_on,
    coalesce(asset_uid is null, true) as violation_00,
    coalesce(asset_uid is not null and count(*) over (partition by asset_uid) > 1, true) as violation_01,
    coalesce(issuer_name_text is null, true) as violation_02
from source_00
