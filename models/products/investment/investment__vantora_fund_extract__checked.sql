-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        vantora_record_id,
        vantora_fund_id,
        fund_name,
        fund_family_name,
        vehicle_type,
        gp_source_id,
        gp_name,
        fund_lei,
        shared_external_id,
        asset_class,
        strategy,
        vintage_year,
        committed_capital_usd,
        currency,
        domicile,
        fund_status,
        as_of_date
    from {{ ref("raw_vantora_funds") }}
)

select
    vantora_record_id,
    vantora_fund_id,
    fund_name,
    fund_family_name,
    vehicle_type,
    gp_source_id,
    gp_name,
    fund_lei,
    shared_external_id,
    asset_class,
    strategy,
    vintage_year,
    committed_capital_usd,
    currency,
    domicile,
    fund_status,
    as_of_date,
    coalesce(vantora_record_id is null, true) as violation_00,
    coalesce(vantora_record_id is not null and count(*) over (partition by vantora_record_id) > 1, true) as violation_01,
    coalesce(vantora_fund_id is null, true) as violation_02,
    coalesce(fund_name is null, true) as violation_03
from source_00
