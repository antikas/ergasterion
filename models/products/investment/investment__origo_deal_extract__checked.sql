-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        deal_id,
        external_deal_id,
        deal_name,
        target_entity_id,
        strategy,
        originating_team,
        source_channel,
        sourced_date,
        structure,
        converted_record_type,
        converted_record_id
    from {{ ref("raw_origo_deals") }}
)

select
    deal_id,
    external_deal_id,
    deal_name,
    target_entity_id,
    strategy,
    originating_team,
    source_channel,
    sourced_date,
    structure,
    converted_record_type,
    converted_record_id,
    coalesce(deal_id is null, true) as violation_00,
    coalesce(deal_id is not null and count(*) over (partition by deal_id) > 1, true) as violation_01,
    coalesce(deal_name is null, true) as violation_02,
    coalesce(sourced_date is null, true) as violation_03
from source_00
