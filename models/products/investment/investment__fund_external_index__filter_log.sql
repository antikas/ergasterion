-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
evaluated as (
    select
        (fund_external_lookup_key is not null) as predicate_00
    from {{ ref("investment__fund_external_index__prefilter") }}
),
counted as (
    select
        'shared_identifier_stated_only' as predicate_name,
        {{ dpf_excluded_count('predicate_00') }} as excluded_count
    from evaluated
)

select
    predicate_name,
    excluded_count
from counted
