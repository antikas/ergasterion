-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        decision_id,
        external_deal_id,
        decision,
        conditions,
        actor,
        decided_at
    from {{ ref("deal_decision_log_fixtures") }}
)

select
    decision_id,
    external_deal_id,
    decision,
    conditions,
    actor,
    decided_at,
    coalesce(decision_id is null, true) as violation_00,
    coalesce(decision_id is not null and count(*) over (partition by decision_id) > 1, true) as violation_01,
    coalesce(external_deal_id is null, true) as violation_02,
    coalesce(decided_at is null, true) as violation_03
from source_00
