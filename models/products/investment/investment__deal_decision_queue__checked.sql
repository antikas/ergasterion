-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        decision_id,
        deal_external_identity,
        decision_outcome,
        decision_conditions,
        decision_actor,
        decision_decided_at,
        decision_rank,
        decision_is_final
    from {{ ref("investment__deal_decision_current") }}
),
step_00_batch_transfer as (
    select
        decision_id,
        deal_external_identity,
        decision_outcome,
        decision_conditions,
        decision_actor,
        decision_decided_at,
        decision_rank,
        decision_is_final
    from source_00
)

select
    decision_id,
    deal_external_identity,
    decision_outcome,
    decision_conditions,
    decision_actor,
    decision_decided_at,
    decision_rank,
    decision_is_final,
    coalesce(decision_id is null, true) as violation_00,
    coalesce(decision_id is not null and count(*) over (partition by decision_id) > 1, true) as violation_01
from step_00_batch_transfer
