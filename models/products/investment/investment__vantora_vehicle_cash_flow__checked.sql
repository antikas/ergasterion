-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        vehicle_cash_flow_id,
        vehicle_id,
        cash_flow_date,
        cash_flow_type,
        direction,
        amount_usd,
        currency
    from {{ ref("investment__vantora_vehicle_cash_flow_extract") }}
),
step_00_batch_transfer as (
    select
        vehicle_cash_flow_id,
        vehicle_id,
        cash_flow_date,
        cash_flow_type,
        direction,
        amount_usd,
        currency
    from source_00
)

select
    vehicle_cash_flow_id,
    vehicle_id,
    cash_flow_date,
    cash_flow_type,
    direction,
    amount_usd,
    currency,
    coalesce(vehicle_cash_flow_id is null, true) as violation_00,
    coalesce(cash_flow_date is null, true) as violation_01
from step_00_batch_transfer
