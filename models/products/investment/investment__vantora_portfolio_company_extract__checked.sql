-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        vantora_company_id,
        vantora_fund_id,
        company_name,
        company_lei,
        shared_external_id,
        sector,
        sub_sector,
        country,
        status,
        first_seen_at
    from {{ ref("raw_vantora_portfolio_companies") }}
)

select
    vantora_company_id,
    vantora_fund_id,
    company_name,
    company_lei,
    shared_external_id,
    sector,
    sub_sector,
    country,
    status,
    first_seen_at,
    coalesce(vantora_company_id is null, true) as violation_00,
    coalesce(vantora_company_id is not null and count(*) over (partition by vantora_company_id) > 1, true) as violation_01,
    coalesce(company_name is null, true) as violation_02
from source_00
