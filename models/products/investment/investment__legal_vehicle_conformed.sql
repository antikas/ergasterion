-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
checked_input as (
    select
        source_vehicle_id,
        source_fund_id,
        source_parent_vehicle_id,
        source_name,
        source_vehicle_type,
        source_jurisdiction,
        source_incorporation_text,
        source_lei,
        source_record_id,
        source_incorporated_on,
        source_reported_on,
        vehicle_source_system,
        vehicle_identity_key,
        vehicle_normalised_name,
        vehicle_lei
    from {{ ref("investment__legal_vehicle_conformed__checked") }}
),
step_02_calculated_fields as (
    select
        source_vehicle_id,
        source_fund_id,
        source_parent_vehicle_id,
        source_name,
        source_vehicle_type,
        source_jurisdiction,
        source_incorporation_text,
        source_lei,
        source_record_id,
        source_incorporated_on,
        source_reported_on,
        vehicle_source_system,
        vehicle_identity_key,
        vehicle_normalised_name,
        vehicle_lei,
        cast((coalesce(vehicle_lei, vehicle_normalised_name)) as {{ dpf_type('string') }}) as vehicle_business_key
    from checked_input
),
step_03_data_enrichment as (
    select
        base.source_vehicle_id,
        base.source_fund_id,
        base.source_parent_vehicle_id,
        base.source_name,
        base.source_vehicle_type,
        base.source_jurisdiction,
        base.source_incorporation_text,
        base.source_lei,
        base.source_record_id,
        base.source_incorporated_on,
        base.source_reported_on,
        base.vehicle_source_system,
        base.vehicle_identity_key,
        base.vehicle_normalised_name,
        base.vehicle_lei,
        base.vehicle_business_key,
        lookup_0.fund_business_key
    from step_02_calculated_fields as base
    left join {{ ref("investment__fund_conformed") }} as lookup_0
        on base.source_fund_id = lookup_0.source_fund_id
       and base.vehicle_source_system = lookup_0.fund_source_system
)

select
    source_vehicle_id,
    source_fund_id,
    source_parent_vehicle_id,
    source_name,
    source_vehicle_type,
    source_jurisdiction,
    source_incorporation_text,
    source_lei,
    source_record_id,
    source_incorporated_on,
    source_reported_on,
    vehicle_source_system,
    vehicle_identity_key,
    vehicle_normalised_name,
    vehicle_lei,
    vehicle_business_key,
    fund_business_key
from step_03_data_enrichment
