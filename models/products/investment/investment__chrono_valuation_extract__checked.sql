-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        mark_uid,
        vehicle_code,
        asset_uid,
        mark_date,
        usd_fair_value,
        mark_method,
        fair_value_bucket,
        extraction_confidence
    from {{ ref("raw_chrono_valuations") }}
)

select
    mark_uid,
    vehicle_code,
    asset_uid,
    mark_date,
    usd_fair_value,
    mark_method,
    fair_value_bucket,
    extraction_confidence,
    coalesce(mark_uid is null, true) as violation_00,
    coalesce(mark_uid is not null and count(*) over (partition by mark_uid) > 1, true) as violation_01,
    coalesce(vehicle_code is null, true) as violation_02,
    coalesce(mark_date is null, true) as violation_03
from source_00
