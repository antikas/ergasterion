-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
checked_input as (
    select
        source_record_id,
        source_gp_id,
        source_name,
        source_lei,
        source_domicile,
        source_reported_on,
        source_external_id,
        source_relationship_start_date,
        gp_source_system,
        gp_identity_key,
        gp_normalised_name,
        gp_lei,
        gp_external_id
    from {{ ref("investment__gp_conformed__checked") }}
),
step_02_calculated_fields as (
    select
        source_record_id,
        source_gp_id,
        source_name,
        source_lei,
        source_domicile,
        source_reported_on,
        source_external_id,
        source_relationship_start_date,
        gp_source_system,
        gp_identity_key,
        gp_normalised_name,
        gp_lei,
        gp_external_id,
        cast((coalesce(gp_lei, gp_external_id, gp_normalised_name)) as {{ dpf_type('string') }}) as gp_business_key
    from checked_input
)

select
    source_record_id,
    source_gp_id,
    source_name,
    source_lei,
    source_domicile,
    source_reported_on,
    source_external_id,
    source_relationship_start_date,
    gp_source_system,
    gp_identity_key,
    gp_normalised_name,
    gp_lei,
    gp_external_id,
    gp_business_key
from step_02_calculated_fields
