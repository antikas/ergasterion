-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
checked_input as (
    select
        vehicle_id,
        meridex_fund_id,
        parent_vehicle_id,
        vehicle_name,
        vehicle_type,
        jurisdiction,
        incorporation_date,
        vehicle_lei
    from {{ ref("investment__meridex_legal_vehicle_extract__checked") }}
)

select
    vehicle_id,
    meridex_fund_id,
    parent_vehicle_id,
    vehicle_name,
    vehicle_type,
    jurisdiction,
    incorporation_date,
    vehicle_lei
from checked_input
