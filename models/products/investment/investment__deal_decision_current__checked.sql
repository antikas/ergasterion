-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        decision_id,
        external_deal_id,
        decision,
        conditions,
        actor,
        decided_at
    from {{ ref("investment__deal_decision_log") }}
),
step_00_batch_transfer as (
    select
        decision_id,
        external_deal_id,
        decision,
        conditions,
        actor,
        decided_at
    from source_00
)

select
    decision_id,
    external_deal_id,
    decision,
    conditions,
    actor,
    decided_at,
    coalesce(decision_id is null, true) as violation_00,
    coalesce(external_deal_id is null, true) as violation_01
from step_00_batch_transfer
