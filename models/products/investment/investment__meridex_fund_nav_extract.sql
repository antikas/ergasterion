-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
checked_input as (
    select
        nav_id,
        meridex_fund_id,
        nav_date,
        nav_usd,
        called_capital_usd,
        distributed_capital_usd
    from {{ ref("investment__meridex_fund_nav_extract__checked") }}
)

select
    nav_id,
    meridex_fund_id,
    nav_date,
    nav_usd,
    called_capital_usd,
    distributed_capital_usd
from checked_input
