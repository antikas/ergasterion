-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        succession_event_id,
        predecessor_gp_id,
        successor_gp_id,
        event_type,
        effective_date
    from {{ ref("raw_vantora_succession_events") }}
)

select
    succession_event_id,
    predecessor_gp_id,
    successor_gp_id,
    event_type,
    effective_date,
    coalesce(succession_event_id is null, true) as violation_00,
    coalesce(succession_event_id is not null and count(*) over (partition by succession_event_id) > 1, true) as violation_01,
    coalesce(predecessor_gp_id is null, true) as violation_02,
    coalesce(successor_gp_id is null, true) as violation_03
from source_00
