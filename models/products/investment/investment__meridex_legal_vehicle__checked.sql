-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        vehicle_id,
        meridex_fund_id,
        parent_vehicle_id,
        vehicle_name,
        vehicle_type,
        jurisdiction,
        incorporation_date,
        vehicle_lei
    from {{ ref("investment__meridex_legal_vehicle_extract") }}
),
step_00_batch_transfer as (
    select
        vehicle_id,
        meridex_fund_id,
        parent_vehicle_id,
        vehicle_name,
        vehicle_type,
        jurisdiction,
        incorporation_date,
        vehicle_lei
    from source_00
)

select
    vehicle_id,
    meridex_fund_id,
    parent_vehicle_id,
    vehicle_name,
    vehicle_type,
    jurisdiction,
    incorporation_date,
    vehicle_lei,
    coalesce(vehicle_id is null, true) as violation_00,
    coalesce(vehicle_name is null, true) as violation_01
from step_00_batch_transfer
