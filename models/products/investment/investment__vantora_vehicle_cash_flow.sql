-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
checked_input as (
    select
        vehicle_cash_flow_id,
        vehicle_id,
        cash_flow_date,
        cash_flow_type,
        direction,
        amount_usd,
        currency
    from {{ ref("investment__vantora_vehicle_cash_flow__checked") }}
),
step_02_schema_transform as (
    select
        cast(vehicle_cash_flow_id as {{ dpf_type('string') }}) as source_record_id,
        cast(vehicle_id as {{ dpf_type('string') }}) as source_vehicle_id,
        cast(cash_flow_date as {{ dpf_type('date') }}) as source_cash_flow_on,
        cast(cash_flow_type as {{ dpf_type('string') }}) as source_cash_flow_type,
        cast(direction as {{ dpf_type('string') }}) as source_direction,
        cast(amount_usd as {{ dpf_decimal_type(18, 2) }}) as source_amount_usd,
        cast(currency as {{ dpf_type('string') }}) as source_currency
    from checked_input
),
step_03_calculated_fields as (
    select
        source_record_id,
        source_vehicle_id,
        source_cash_flow_on,
        source_cash_flow_type,
        source_direction,
        source_amount_usd,
        source_currency,
        cast(('vantora') as {{ dpf_type('string') }}) as vehicle_cash_flow_source_system
    from step_02_schema_transform
)

select
    source_record_id,
    source_vehicle_id,
    source_cash_flow_on,
    source_cash_flow_type,
    source_direction,
    source_amount_usd,
    source_currency,
    vehicle_cash_flow_source_system
from step_03_calculated_fields
