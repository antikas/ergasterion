-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
checked_input as (
    select
        mark_uid,
        vehicle_code,
        asset_uid,
        mark_date,
        usd_fair_value,
        mark_method,
        fair_value_bucket,
        extraction_confidence
    from {{ ref("investment__chrono_valuation__checked") }}
),
step_02_schema_transform as (
    select
        cast(mark_uid as {{ dpf_type('string') }}) as source_record_id,
        cast(vehicle_code as {{ dpf_type('string') }}) as source_fund_id,
        cast(asset_uid as {{ dpf_type('string') }}) as source_company_id,
        cast(mark_date as {{ dpf_type('date') }}) as source_valued_on,
        cast(usd_fair_value as {{ dpf_decimal_type(18, 2) }}) as source_value_usd,
        cast(mark_method as {{ dpf_type('string') }}) as source_method,
        cast(fair_value_bucket as {{ dpf_type('string') }}) as source_valuation_level,
        cast(extraction_confidence as {{ dpf_decimal_type(5, 4) }}) as source_confidence_score
    from checked_input
),
step_03_calculated_fields as (
    select
        source_record_id,
        source_fund_id,
        source_company_id,
        source_valued_on,
        source_value_usd,
        source_method,
        source_valuation_level,
        source_confidence_score,
        cast(('chrono') as {{ dpf_type('string') }}) as valuation_source_system
    from step_02_schema_transform
)

select
    source_record_id,
    source_fund_id,
    source_company_id,
    source_valued_on,
    source_value_usd,
    source_method,
    source_valuation_level,
    source_confidence_score,
    valuation_source_system
from step_03_calculated_fields
