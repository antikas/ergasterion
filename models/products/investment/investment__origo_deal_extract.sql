-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
checked_input as (
    select
        deal_id,
        external_deal_id,
        deal_name,
        target_entity_id,
        strategy,
        originating_team,
        source_channel,
        sourced_date,
        structure,
        converted_record_type,
        converted_record_id
    from {{ ref("investment__origo_deal_extract__checked") }}
)

select
    deal_id,
    external_deal_id,
    deal_name,
    target_entity_id,
    strategy,
    originating_team,
    source_channel,
    sourced_date,
    structure,
    converted_record_type,
    converted_record_id
from checked_input
