-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        vantora_gp_id,
        gp_name,
        gp_lei,
        shared_external_id,
        domicile,
        relationship_start_date
    from {{ ref("raw_vantora_gps") }}
)

select
    vantora_gp_id,
    gp_name,
    gp_lei,
    shared_external_id,
    domicile,
    relationship_start_date,
    coalesce(vantora_gp_id is null, true) as violation_00,
    coalesce(vantora_gp_id is not null and count(*) over (partition by vantora_gp_id) > 1, true) as violation_01,
    coalesce(gp_name is null, true) as violation_02
from source_00
