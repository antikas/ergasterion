-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
resolution_input as (
    select
        vehicle_source_system,
        vehicle_identity_key,
        vehicle_normalised_name,
        vehicle_lei,
        vehicle_business_key,
        vehicle_record_id,
        vehicle_source_id,
        vehicle_fund_source_id,
        vehicle_parent_source_id,
        vehicle_name,
        vehicle_type,
        vehicle_jurisdiction,
        vehicle_incorporated_on,
        vehicle_reported_on,
        vehicle_fund_business_key,
        resolution_record_key,
        resolution_entity_key
    from {{ ref("investment__legal_vehicle_vault__resolution") }}
),
survivorship_vehicle_source_system as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            vehicle_source_system as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when vehicle_source_system is null then 1 else 0 end, vehicle_reported_on desc, case when vehicle_source_system = 'vantora' then 1 when vehicle_source_system = 'meridex' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_vehicle_source_system
    where pick_rank = 1
),
survivorship_vehicle_identity_key as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            vehicle_identity_key as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when vehicle_identity_key is null then 1 else 0 end, vehicle_reported_on desc, case when vehicle_source_system = 'vantora' then 1 when vehicle_source_system = 'meridex' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_vehicle_identity_key
    where pick_rank = 1
),
survivorship_vehicle_normalised_name as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            vehicle_normalised_name as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when vehicle_normalised_name is null then 1 else 0 end, vehicle_reported_on desc, case when vehicle_source_system = 'vantora' then 1 when vehicle_source_system = 'meridex' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_vehicle_normalised_name
    where pick_rank = 1
),
survivorship_vehicle_lei as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            vehicle_lei as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when vehicle_lei is null then 1 else 0 end, case when vehicle_source_system = 'vantora' then 1 when vehicle_source_system = 'meridex' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_vehicle_lei
    where pick_rank = 1
),
survivorship_vehicle_business_key as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            vehicle_business_key as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when vehicle_business_key is null then 1 else 0 end, case when vehicle_source_system = 'vantora' then 1 when vehicle_source_system = 'meridex' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_vehicle_business_key
    where pick_rank = 1
),
survivorship_vehicle_record_id as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            vehicle_record_id as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when vehicle_record_id is null then 1 else 0 end, vehicle_reported_on desc, case when vehicle_source_system = 'vantora' then 1 when vehicle_source_system = 'meridex' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_vehicle_record_id
    where pick_rank = 1
),
survivorship_vehicle_source_id as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            vehicle_source_id as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when vehicle_source_id is null then 1 else 0 end, case when vehicle_source_system = 'vantora' then 1 when vehicle_source_system = 'meridex' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_vehicle_source_id
    where pick_rank = 1
),
survivorship_vehicle_fund_source_id as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            vehicle_fund_source_id as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when vehicle_fund_source_id is null then 1 else 0 end, case when vehicle_source_system = 'vantora' then 1 when vehicle_source_system = 'meridex' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_vehicle_fund_source_id
    where pick_rank = 1
),
survivorship_vehicle_parent_source_id as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            vehicle_parent_source_id as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when vehicle_parent_source_id is null then 1 else 0 end, case when vehicle_source_system = 'vantora' then 1 when vehicle_source_system = 'meridex' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_vehicle_parent_source_id
    where pick_rank = 1
),
survivorship_vehicle_name as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            vehicle_name as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when vehicle_name is null then 1 else 0 end, vehicle_reported_on desc, case when vehicle_source_system = 'vantora' then 1 when vehicle_source_system = 'meridex' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_vehicle_name
    where pick_rank = 1
),
survivorship_vehicle_type as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            vehicle_type as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when vehicle_type is null then 1 else 0 end, vehicle_reported_on desc, case when vehicle_source_system = 'vantora' then 1 when vehicle_source_system = 'meridex' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_vehicle_type
    where pick_rank = 1
),
survivorship_vehicle_jurisdiction as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            vehicle_jurisdiction as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when vehicle_jurisdiction is null then 1 else 0 end, case when vehicle_source_system = 'vantora' then 1 when vehicle_source_system = 'meridex' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_vehicle_jurisdiction
    where pick_rank = 1
),
survivorship_vehicle_incorporated_on as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            vehicle_incorporated_on as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when vehicle_incorporated_on is null then 1 else 0 end, case when vehicle_source_system = 'vantora' then 1 when vehicle_source_system = 'meridex' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_vehicle_incorporated_on
    where pick_rank = 1
),
survivorship_vehicle_reported_on as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            vehicle_reported_on as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when vehicle_reported_on is null then 1 else 0 end, vehicle_reported_on desc, case when vehicle_source_system = 'vantora' then 1 when vehicle_source_system = 'meridex' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_vehicle_reported_on
    where pick_rank = 1
),
survivorship_vehicle_fund_business_key as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            vehicle_fund_business_key as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when vehicle_fund_business_key is null then 1 else 0 end, case when vehicle_source_system = 'vantora' then 1 when vehicle_source_system = 'meridex' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_vehicle_fund_business_key
    where pick_rank = 1
),
curation_entities as (
    select distinct
        resolution_entity_key
    from resolution_input
),
curation_survivor as (
    select
        survivorship_vehicle_source_system.attribute_value as vehicle_source_system,
        survivorship_vehicle_identity_key.attribute_value as vehicle_identity_key,
        survivorship_vehicle_normalised_name.attribute_value as vehicle_normalised_name,
        survivorship_vehicle_lei.attribute_value as vehicle_lei,
        survivorship_vehicle_business_key.attribute_value as vehicle_business_key,
        survivorship_vehicle_record_id.attribute_value as vehicle_record_id,
        survivorship_vehicle_source_id.attribute_value as vehicle_source_id,
        survivorship_vehicle_fund_source_id.attribute_value as vehicle_fund_source_id,
        survivorship_vehicle_parent_source_id.attribute_value as vehicle_parent_source_id,
        survivorship_vehicle_name.attribute_value as vehicle_name,
        survivorship_vehicle_type.attribute_value as vehicle_type,
        survivorship_vehicle_jurisdiction.attribute_value as vehicle_jurisdiction,
        survivorship_vehicle_incorporated_on.attribute_value as vehicle_incorporated_on,
        survivorship_vehicle_reported_on.attribute_value as vehicle_reported_on,
        survivorship_vehicle_fund_business_key.attribute_value as vehicle_fund_business_key
    from curation_entities as entities
    left join survivorship_vehicle_source_system
        on survivorship_vehicle_source_system.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_vehicle_identity_key
        on survivorship_vehicle_identity_key.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_vehicle_normalised_name
        on survivorship_vehicle_normalised_name.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_vehicle_lei
        on survivorship_vehicle_lei.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_vehicle_business_key
        on survivorship_vehicle_business_key.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_vehicle_record_id
        on survivorship_vehicle_record_id.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_vehicle_source_id
        on survivorship_vehicle_source_id.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_vehicle_fund_source_id
        on survivorship_vehicle_fund_source_id.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_vehicle_parent_source_id
        on survivorship_vehicle_parent_source_id.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_vehicle_name
        on survivorship_vehicle_name.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_vehicle_type
        on survivorship_vehicle_type.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_vehicle_jurisdiction
        on survivorship_vehicle_jurisdiction.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_vehicle_incorporated_on
        on survivorship_vehicle_incorporated_on.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_vehicle_reported_on
        on survivorship_vehicle_reported_on.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_vehicle_fund_business_key
        on survivorship_vehicle_fund_business_key.resolution_entity_key = entities.resolution_entity_key
)

select
    vehicle_source_system,
    vehicle_identity_key,
    vehicle_normalised_name,
    vehicle_lei,
    vehicle_business_key,
    vehicle_record_id,
    vehicle_source_id,
    vehicle_fund_source_id,
    vehicle_parent_source_id,
    vehicle_name,
    vehicle_type,
    vehicle_jurisdiction,
    vehicle_incorporated_on,
    vehicle_reported_on,
    vehicle_fund_business_key,
    coalesce(vehicle_business_key is null, true) as violation_00,
    coalesce(vehicle_business_key is not null and count(*) over (partition by vehicle_business_key) > 1, true) as violation_01
from curation_survivor
