-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
resolution_input as (
    select
        deal_source_system,
        deal_identity_key,
        deal_normalised_name,
        deal_external_id,
        deal_business_key,
        deal_target_company_external_id,
        deal_converted_fund_external_id,
        deal_record_id,
        deal_name,
        deal_strategy,
        deal_originating_team,
        deal_channel,
        deal_sourced_on,
        deal_structure,
        deal_converted_record_type,
        deal_company_business_key,
        deal_fund_business_key,
        deal_external_identity,
        resolution_record_key,
        resolution_entity_key
    from {{ ref("investment__deal_vault__resolution") }}
),
survivorship_deal_source_system as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            deal_source_system as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when deal_source_system is null then 1 else 0 end, deal_sourced_on desc, case when deal_source_system = 'origo' then 1 else 2 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_deal_source_system
    where pick_rank = 1
),
survivorship_deal_identity_key as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            deal_identity_key as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when deal_identity_key is null then 1 else 0 end, deal_sourced_on desc, case when deal_source_system = 'origo' then 1 else 2 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_deal_identity_key
    where pick_rank = 1
),
survivorship_deal_normalised_name as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            deal_normalised_name as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when deal_normalised_name is null then 1 else 0 end, deal_sourced_on desc, case when deal_source_system = 'origo' then 1 else 2 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_deal_normalised_name
    where pick_rank = 1
),
survivorship_deal_external_id as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            deal_external_id as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when deal_external_id is null then 1 else 0 end, case when deal_source_system = 'origo' then 1 else 2 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_deal_external_id
    where pick_rank = 1
),
survivorship_deal_business_key as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            deal_business_key as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when deal_business_key is null then 1 else 0 end, case when deal_source_system = 'origo' then 1 else 2 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_deal_business_key
    where pick_rank = 1
),
survivorship_deal_target_company_external_id as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            deal_target_company_external_id as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when deal_target_company_external_id is null then 1 else 0 end, case when deal_source_system = 'origo' then 1 else 2 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_deal_target_company_external_id
    where pick_rank = 1
),
survivorship_deal_converted_fund_external_id as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            deal_converted_fund_external_id as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when deal_converted_fund_external_id is null then 1 else 0 end, case when deal_source_system = 'origo' then 1 else 2 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_deal_converted_fund_external_id
    where pick_rank = 1
),
survivorship_deal_record_id as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            deal_record_id as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when deal_record_id is null then 1 else 0 end, deal_sourced_on desc, case when deal_source_system = 'origo' then 1 else 2 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_deal_record_id
    where pick_rank = 1
),
survivorship_deal_name as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            deal_name as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when deal_name is null then 1 else 0 end, deal_sourced_on desc, case when deal_source_system = 'origo' then 1 else 2 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_deal_name
    where pick_rank = 1
),
survivorship_deal_strategy as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            deal_strategy as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when deal_strategy is null then 1 else 0 end, deal_sourced_on desc, case when deal_source_system = 'origo' then 1 else 2 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_deal_strategy
    where pick_rank = 1
),
survivorship_deal_originating_team as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            deal_originating_team as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when deal_originating_team is null then 1 else 0 end, case when deal_source_system = 'origo' then 1 else 2 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_deal_originating_team
    where pick_rank = 1
),
survivorship_deal_channel as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            deal_channel as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when deal_channel is null then 1 else 0 end, case when deal_source_system = 'origo' then 1 else 2 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_deal_channel
    where pick_rank = 1
),
survivorship_deal_sourced_on as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            deal_sourced_on as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when deal_sourced_on is null then 1 else 0 end, deal_sourced_on desc, case when deal_source_system = 'origo' then 1 else 2 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_deal_sourced_on
    where pick_rank = 1
),
survivorship_deal_structure as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            deal_structure as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when deal_structure is null then 1 else 0 end, deal_sourced_on desc, case when deal_source_system = 'origo' then 1 else 2 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_deal_structure
    where pick_rank = 1
),
survivorship_deal_converted_record_type as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            deal_converted_record_type as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when deal_converted_record_type is null then 1 else 0 end, case when deal_source_system = 'origo' then 1 else 2 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_deal_converted_record_type
    where pick_rank = 1
),
survivorship_deal_company_business_key as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            deal_company_business_key as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when deal_company_business_key is null then 1 else 0 end, case when deal_source_system = 'origo' then 1 else 2 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_deal_company_business_key
    where pick_rank = 1
),
survivorship_deal_fund_business_key as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            deal_fund_business_key as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when deal_fund_business_key is null then 1 else 0 end, case when deal_source_system = 'origo' then 1 else 2 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_deal_fund_business_key
    where pick_rank = 1
),
survivorship_deal_external_identity as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            deal_external_identity as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when deal_external_identity is null then 1 else 0 end, case when deal_source_system = 'origo' then 1 else 2 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_deal_external_identity
    where pick_rank = 1
),
curation_entities as (
    select distinct
        resolution_entity_key
    from resolution_input
),
curation_survivor as (
    select
        survivorship_deal_source_system.attribute_value as deal_source_system,
        survivorship_deal_identity_key.attribute_value as deal_identity_key,
        survivorship_deal_normalised_name.attribute_value as deal_normalised_name,
        survivorship_deal_external_id.attribute_value as deal_external_id,
        survivorship_deal_business_key.attribute_value as deal_business_key,
        survivorship_deal_target_company_external_id.attribute_value as deal_target_company_external_id,
        survivorship_deal_converted_fund_external_id.attribute_value as deal_converted_fund_external_id,
        survivorship_deal_record_id.attribute_value as deal_record_id,
        survivorship_deal_name.attribute_value as deal_name,
        survivorship_deal_strategy.attribute_value as deal_strategy,
        survivorship_deal_originating_team.attribute_value as deal_originating_team,
        survivorship_deal_channel.attribute_value as deal_channel,
        survivorship_deal_sourced_on.attribute_value as deal_sourced_on,
        survivorship_deal_structure.attribute_value as deal_structure,
        survivorship_deal_converted_record_type.attribute_value as deal_converted_record_type,
        survivorship_deal_company_business_key.attribute_value as deal_company_business_key,
        survivorship_deal_fund_business_key.attribute_value as deal_fund_business_key,
        survivorship_deal_external_identity.attribute_value as deal_external_identity
    from curation_entities as entities
    left join survivorship_deal_source_system
        on survivorship_deal_source_system.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_deal_identity_key
        on survivorship_deal_identity_key.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_deal_normalised_name
        on survivorship_deal_normalised_name.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_deal_external_id
        on survivorship_deal_external_id.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_deal_business_key
        on survivorship_deal_business_key.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_deal_target_company_external_id
        on survivorship_deal_target_company_external_id.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_deal_converted_fund_external_id
        on survivorship_deal_converted_fund_external_id.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_deal_record_id
        on survivorship_deal_record_id.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_deal_name
        on survivorship_deal_name.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_deal_strategy
        on survivorship_deal_strategy.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_deal_originating_team
        on survivorship_deal_originating_team.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_deal_channel
        on survivorship_deal_channel.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_deal_sourced_on
        on survivorship_deal_sourced_on.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_deal_structure
        on survivorship_deal_structure.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_deal_converted_record_type
        on survivorship_deal_converted_record_type.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_deal_company_business_key
        on survivorship_deal_company_business_key.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_deal_fund_business_key
        on survivorship_deal_fund_business_key.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_deal_external_identity
        on survivorship_deal_external_identity.resolution_entity_key = entities.resolution_entity_key
)

select
    deal_source_system,
    deal_identity_key,
    deal_normalised_name,
    deal_external_id,
    deal_business_key,
    deal_target_company_external_id,
    deal_converted_fund_external_id,
    deal_record_id,
    deal_name,
    deal_strategy,
    deal_originating_team,
    deal_channel,
    deal_sourced_on,
    deal_structure,
    deal_converted_record_type,
    deal_company_business_key,
    deal_fund_business_key,
    deal_external_identity,
    coalesce(deal_business_key is null, true) as violation_00,
    coalesce(deal_business_key is not null and count(*) over (partition by deal_business_key) > 1, true) as violation_01
from curation_survivor
