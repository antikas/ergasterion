-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
checked_pre_input as (
    select
        source_record_id,
        source_gp_id,
        source_name,
        source_lei,
        source_domicile,
        source_reported_on,
        source_external_id,
        source_relationship_start_date,
        gp_source_system,
        gp_identity_key,
        gp_normalised_name,
        gp_lei,
        gp_external_id,
        gp_business_key
    from {{ ref("investment__gp_vault__checked_pre") }}
),
step_02_schema_transform as (
    select
        gp_source_system,
        gp_identity_key,
        gp_normalised_name,
        gp_lei,
        gp_external_id,
        gp_business_key,
        cast(source_record_id as {{ dpf_type('string') }}) as gp_record_id,
        cast(source_gp_id as {{ dpf_type('string') }}) as gp_source_id,
        cast(source_name as {{ dpf_type('string') }}) as gp_name,
        cast(source_domicile as {{ dpf_type('string') }}) as gp_domicile,
        cast(source_relationship_start_date as {{ dpf_type('date') }}) as gp_relationship_start_on,
        cast(source_reported_on as {{ dpf_type('date') }}) as gp_reported_on
    from checked_pre_input
),
curation_records as (
    select
        gp_source_system,
        gp_identity_key,
        gp_normalised_name,
        gp_lei,
        gp_external_id,
        gp_business_key,
        gp_record_id,
        gp_source_id,
        gp_name,
        gp_domicile,
        gp_relationship_start_on,
        gp_reported_on,
        gp_identity_key as resolution_record_key
    from step_02_schema_transform
),
curation_edges as (
    select
        resolution_record_key,
        1 as key_rank,
        'gp_lei' as key_name,
        'gp_lei:' || gp_lei as key_value
    from curation_records
    where gp_lei is not null

    union all

    select
        resolution_record_key,
        2 as key_rank,
        'gp_external_id' as key_name,
        'gp_external_id:' || gp_external_id as key_value
    from curation_records
    where gp_external_id is not null

    union all

    select
        resolution_record_key,
        3 as key_rank,
        'gp_normalised_name' as key_name,
        'gp_normalised_name:' || gp_normalised_name as key_value
    from curation_records
    where gp_normalised_name is not null
),
curation_labels_0 as (
    select distinct
        resolution_record_key,
        resolution_record_key as label
    from curation_edges
),
curation_values_1 as (
    select
        edges.key_value,
        min(labelled.label) as value_label
    from curation_edges as edges
    inner join curation_labels_0 as labelled
        on labelled.resolution_record_key = edges.resolution_record_key
    group by edges.key_value
),
curation_labels_1 as (
    select
        edges.resolution_record_key,
        min(matched.value_label) as label
    from curation_edges as edges
    inner join curation_values_1 as matched
        on matched.key_value = edges.key_value
    group by edges.resolution_record_key
),
curation_values_2 as (
    select
        edges.key_value,
        min(labelled.label) as value_label
    from curation_edges as edges
    inner join curation_labels_1 as labelled
        on labelled.resolution_record_key = edges.resolution_record_key
    group by edges.key_value
),
curation_labels_2 as (
    select
        edges.resolution_record_key,
        min(matched.value_label) as label
    from curation_edges as edges
    inner join curation_values_2 as matched
        on matched.key_value = edges.key_value
    group by edges.resolution_record_key
),
curation_values_3 as (
    select
        edges.key_value,
        min(labelled.label) as value_label
    from curation_edges as edges
    inner join curation_labels_2 as labelled
        on labelled.resolution_record_key = edges.resolution_record_key
    group by edges.key_value
),
curation_labels_3 as (
    select
        edges.resolution_record_key,
        min(matched.value_label) as label
    from curation_edges as edges
    inner join curation_values_3 as matched
        on matched.key_value = edges.key_value
    group by edges.resolution_record_key
),
curation_components as (
    select
        resolution_record_key,
        label as component_id
    from curation_labels_3
),
curation_component_stats as (
    select
        component_id,
        count(*) as component_row_count
    from curation_components
    group by component_id
),
curation_component_keys as (
    select
        components.component_id,
        min(edges.key_rank) as best_key_rank
    from curation_components as components
    inner join curation_edges as edges
        on edges.resolution_record_key = components.resolution_record_key
    group by components.component_id
),
curation_resolved as (
    select
        records.gp_source_system,
        records.gp_identity_key,
        records.gp_normalised_name,
        records.gp_lei,
        records.gp_external_id,
        records.gp_business_key,
        records.gp_record_id,
        records.gp_source_id,
        records.gp_name,
        records.gp_domicile,
        records.gp_relationship_start_on,
        records.gp_reported_on,
        records.resolution_record_key,
        coalesce(components.component_id, records.resolution_record_key) as resolution_entity_key,
        coalesce(stats.component_row_count, 1) as resolution_row_count,
        case
            when keys.best_key_rank = 1 then 'gp_lei'
            when keys.best_key_rank = 2 then 'gp_external_id'
            when keys.best_key_rank = 3 then 'gp_normalised_name'
        end as resolution_key_name,
        case
            when coalesce(stats.component_row_count, 1) > 1 then 'deterministic'
            else 'unresolved'
        end as resolution_tier,
        coalesce(stats.component_row_count, 1) = 1 as resolution_pending
    from curation_records as records
    left join curation_components as components
        on components.resolution_record_key = records.resolution_record_key
    left join curation_component_stats as stats
        on stats.component_id = components.component_id
    left join curation_component_keys as keys
        on keys.component_id = components.component_id
)

select
    gp_source_system,
    gp_identity_key,
    gp_normalised_name,
    gp_lei,
    gp_external_id,
    gp_business_key,
    gp_record_id,
    gp_source_id,
    gp_name,
    gp_domicile,
    gp_relationship_start_on,
    gp_reported_on,
    resolution_record_key,
    resolution_entity_key,
    resolution_row_count,
    resolution_key_name,
    resolution_tier,
    resolution_pending
from curation_resolved
