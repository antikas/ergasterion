-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        meridex_company_id,
        meridex_fund_id,
        company_name,
        company_lei,
        shared_external_id,
        sector,
        country
    from {{ ref("investment__meridex_portfolio_company_extract") }}
),
step_00_batch_transfer as (
    select
        meridex_company_id,
        meridex_fund_id,
        company_name,
        company_lei,
        shared_external_id,
        sector,
        country
    from source_00
)

select
    meridex_company_id,
    meridex_fund_id,
    company_name,
    company_lei,
    shared_external_id,
    sector,
    country,
    coalesce(meridex_company_id is null, true) as violation_00,
    coalesce(company_name is null, true) as violation_01
from step_00_batch_transfer
