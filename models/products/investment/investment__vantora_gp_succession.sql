-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
checked_input as (
    select
        succession_event_id,
        predecessor_gp_id,
        successor_gp_id,
        event_type,
        effective_date
    from {{ ref("investment__vantora_gp_succession__checked") }}
),
step_02_schema_transform as (
    select
        cast(succession_event_id as {{ dpf_type('string') }}) as source_record_id,
        cast(predecessor_gp_id as {{ dpf_type('string') }}) as source_predecessor_gp_id,
        cast(successor_gp_id as {{ dpf_type('string') }}) as source_successor_gp_id,
        cast(event_type as {{ dpf_type('string') }}) as source_event_type,
        cast(effective_date as {{ dpf_type('date') }}) as source_effective_on
    from checked_input
),
step_03_calculated_fields as (
    select
        source_record_id,
        source_predecessor_gp_id,
        source_successor_gp_id,
        source_event_type,
        source_effective_on,
        cast(('vantora') as {{ dpf_type('string') }}) as succession_source_system
    from step_02_schema_transform
)

select
    source_record_id,
    source_predecessor_gp_id,
    source_successor_gp_id,
    source_event_type,
    source_effective_on,
    succession_source_system
from step_03_calculated_fields
