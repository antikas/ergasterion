-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
checked_input as (
    select
        valuation_id,
        vantora_fund_id,
        vantora_company_id,
        valuation_date,
        value_usd,
        method,
        valuation_level,
        confidence_score
    from {{ ref("investment__vantora_valuation__checked") }}
),
step_02_schema_transform as (
    select
        cast(valuation_id as {{ dpf_type('string') }}) as source_record_id,
        cast(vantora_fund_id as {{ dpf_type('string') }}) as source_fund_id,
        cast(vantora_company_id as {{ dpf_type('string') }}) as source_company_id,
        cast(valuation_date as {{ dpf_type('date') }}) as source_valued_on,
        cast(value_usd as {{ dpf_decimal_type(18, 2) }}) as source_value_usd,
        cast(method as {{ dpf_type('string') }}) as source_method,
        cast(valuation_level as {{ dpf_type('string') }}) as source_valuation_level,
        cast(confidence_score as {{ dpf_decimal_type(5, 4) }}) as source_confidence_score
    from checked_input
),
step_03_calculated_fields as (
    select
        source_record_id,
        source_fund_id,
        source_company_id,
        source_valued_on,
        source_value_usd,
        source_method,
        source_valuation_level,
        source_confidence_score,
        cast(('vantora') as {{ dpf_type('string') }}) as valuation_source_system
    from step_02_schema_transform
)

select
    source_record_id,
    source_fund_id,
    source_company_id,
    source_valued_on,
    source_value_usd,
    source_method,
    source_valuation_level,
    source_confidence_score,
    valuation_source_system
from step_03_calculated_fields
