-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        nav_id,
        meridex_fund_id,
        nav_date,
        nav_usd,
        called_capital_usd,
        distributed_capital_usd
    from {{ ref("raw_meridex_fund_nav") }}
)

select
    nav_id,
    meridex_fund_id,
    nav_date,
    nav_usd,
    called_capital_usd,
    distributed_capital_usd,
    coalesce(nav_id is null, true) as violation_00,
    coalesce(nav_id is not null and count(*) over (partition by nav_id) > 1, true) as violation_01,
    coalesce(meridex_fund_id is null, true) as violation_02,
    coalesce(nav_date is null, true) as violation_03
from source_00
