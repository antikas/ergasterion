-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        cash_flow_id,
        meridex_fund_id,
        cash_flow_date,
        cash_flow_type,
        direction,
        amount_usd,
        currency
    from {{ ref("investment__meridex_cash_flow_extract") }}
),
step_00_batch_transfer as (
    select
        cash_flow_id,
        meridex_fund_id,
        cash_flow_date,
        cash_flow_type,
        direction,
        amount_usd,
        currency
    from source_00
)

select
    cash_flow_id,
    meridex_fund_id,
    cash_flow_date,
    cash_flow_type,
    direction,
    amount_usd,
    currency,
    coalesce(cash_flow_id is null, true) as violation_00,
    coalesce(cash_flow_date is null, true) as violation_01
from step_00_batch_transfer
