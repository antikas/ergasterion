-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        vantora_gp_id,
        gp_name,
        gp_lei,
        shared_external_id,
        domicile,
        relationship_start_date
    from {{ ref("investment__vantora_gp_extract") }}
),
step_00_batch_transfer as (
    select
        vantora_gp_id,
        gp_name,
        gp_lei,
        shared_external_id,
        domicile,
        relationship_start_date
    from source_00
)

select
    vantora_gp_id,
    gp_name,
    gp_lei,
    shared_external_id,
    domicile,
    relationship_start_date,
    coalesce(vantora_gp_id is null, true) as violation_00,
    coalesce(gp_name is null, true) as violation_01
from step_00_batch_transfer
