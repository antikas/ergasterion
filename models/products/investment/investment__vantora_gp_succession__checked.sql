-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        succession_event_id,
        predecessor_gp_id,
        successor_gp_id,
        event_type,
        effective_date
    from {{ ref("investment__vantora_succession_event_extract") }}
),
step_00_batch_transfer as (
    select
        succession_event_id,
        predecessor_gp_id,
        successor_gp_id,
        event_type,
        effective_date
    from source_00
)

select
    succession_event_id,
    predecessor_gp_id,
    successor_gp_id,
    event_type,
    effective_date,
    coalesce(succession_event_id is null, true) as violation_00,
    coalesce(effective_date is null, true) as violation_01
from step_00_batch_transfer
