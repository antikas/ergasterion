-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        event_uid,
        vehicle_code,
        event_date,
        event_kind,
        investor_direction,
        usd_amount,
        currency_code
    from {{ ref("investment__chrono_cash_flow_extract") }}
),
step_00_batch_transfer as (
    select
        event_uid,
        vehicle_code,
        event_date,
        event_kind,
        investor_direction,
        usd_amount,
        currency_code
    from source_00
)

select
    event_uid,
    vehicle_code,
    event_date,
    event_kind,
    investor_direction,
    usd_amount,
    currency_code,
    coalesce(event_uid is null, true) as violation_00,
    coalesce(event_date is null, true) as violation_01
from step_00_batch_transfer
