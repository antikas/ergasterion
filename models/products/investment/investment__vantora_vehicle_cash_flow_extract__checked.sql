-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        vehicle_cash_flow_id,
        vehicle_id,
        cash_flow_date,
        cash_flow_type,
        direction,
        amount_usd,
        currency
    from {{ ref("raw_vantora_vehicle_cash_flows") }}
)

select
    vehicle_cash_flow_id,
    vehicle_id,
    cash_flow_date,
    cash_flow_type,
    direction,
    amount_usd,
    currency,
    coalesce(vehicle_cash_flow_id is null, true) as violation_00,
    coalesce(vehicle_cash_flow_id is not null and count(*) over (partition by vehicle_cash_flow_id) > 1, true) as violation_01,
    coalesce(vehicle_id is null, true) as violation_02,
    coalesce(cash_flow_date is null, true) as violation_03
from source_00
