-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        nav_id,
        meridex_fund_id,
        nav_date,
        nav_usd,
        called_capital_usd,
        distributed_capital_usd
    from {{ ref("investment__meridex_fund_nav_extract") }}
),
step_00_batch_transfer as (
    select
        nav_id,
        meridex_fund_id,
        nav_date,
        nav_usd,
        called_capital_usd,
        distributed_capital_usd
    from source_00
)

select
    nav_id,
    meridex_fund_id,
    nav_date,
    nav_usd,
    called_capital_usd,
    distributed_capital_usd,
    coalesce(nav_id is null, true) as violation_00,
    coalesce(nav_date is null, true) as violation_01
from step_00_batch_transfer
