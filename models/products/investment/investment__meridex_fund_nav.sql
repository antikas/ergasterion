-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
checked_input as (
    select
        nav_id,
        meridex_fund_id,
        nav_date,
        nav_usd,
        called_capital_usd,
        distributed_capital_usd
    from {{ ref("investment__meridex_fund_nav__checked") }}
),
step_02_schema_transform as (
    select
        cast(nav_id as {{ dpf_type('string') }}) as source_record_id,
        cast(meridex_fund_id as {{ dpf_type('string') }}) as source_fund_id,
        cast(nav_date as {{ dpf_type('date') }}) as source_valued_on,
        cast(nav_usd as {{ dpf_decimal_type(18, 2) }}) as source_nav_usd,
        cast(called_capital_usd as {{ dpf_decimal_type(18, 2) }}) as source_called_capital_usd,
        cast(distributed_capital_usd as {{ dpf_decimal_type(18, 2) }}) as source_distributed_capital_usd
    from checked_input
),
step_03_calculated_fields as (
    select
        source_record_id,
        source_fund_id,
        source_valued_on,
        source_nav_usd,
        source_called_capital_usd,
        source_distributed_capital_usd,
        cast(('meridex') as {{ dpf_type('string') }}) as nav_source_system
    from step_02_schema_transform
)

select
    source_record_id,
    source_fund_id,
    source_valued_on,
    source_nav_usd,
    source_called_capital_usd,
    source_distributed_capital_usd,
    nav_source_system
from step_03_calculated_fields
