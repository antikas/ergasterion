-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
gp_source as (
    select
        external_ids,
        lei,
        gp_name,
        domicile,
        relationship_start_date
    from {{ ref("investment__gp_canonical__base") }}
)

select
    cast(external_ids as {{ dpf_type('string') }}) as external_ids,
    cast(lei as {{ dpf_type('string') }}) as lei,
    cast(gp_name as {{ dpf_type('string') }}) as gp_name,
    cast(domicile as {{ dpf_type('string') }}) as domicile,
    cast(relationship_start_date as {{ dpf_type('date') }}) as relationship_start_date
from gp_source
