-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
checked_input as (
    select
        legal_vehicle_hk,
        vehicle_business_key,
        vehicle_name,
        vehicle_type,
        vehicle_jurisdiction,
        vehicle_incorporated_on,
        vehicle_fund_source_id,
        vehicle_parent_source_id,
        vehicle_lei,
        vehicle_fund_business_key,
        load_datetime,
        record_source
    from {{ ref("investment__legal_vehicle_canonical__checked") }}
),
step_02_schema_transform as (
    select
        cast(vehicle_business_key as {{ dpf_type('string') }}) as vehicle_id,
        cast(vehicle_lei as {{ dpf_type('string') }}) as lei,
        cast(vehicle_name as {{ dpf_type('string') }}) as vehicle_name,
        cast(vehicle_type as {{ dpf_type('string') }}) as vehicle_type,
        cast(vehicle_jurisdiction as {{ dpf_type('string') }}) as jurisdiction,
        cast(vehicle_incorporated_on as {{ dpf_type('date') }}) as incorporation_date,
        cast(vehicle_fund_business_key as {{ dpf_type('string') }}) as investment_id
    from checked_input
)

select
    vehicle_id,
    lei,
    vehicle_name,
    vehicle_type,
    jurisdiction,
    incorporation_date,
    investment_id
from step_02_schema_transform
