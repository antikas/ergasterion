-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
portfolio_company_source as (
    select
        external_ids,
        lei,
        company_name,
        sector,
        sub_sector,
        country,
        status
    from {{ ref("investment__portfolio_company_canonical__base") }}
)

select
    cast(external_ids as {{ dpf_type('string') }}) as external_ids,
    cast(lei as {{ dpf_type('string') }}) as lei,
    cast(company_name as {{ dpf_type('string') }}) as company_name,
    cast(sector as {{ dpf_type('string') }}) as sector,
    cast(sub_sector as {{ dpf_type('string') }}) as sub_sector,
    cast(country as {{ dpf_type('string') }}) as country,
    cast(status as {{ dpf_type('string') }}) as status
from portfolio_company_source
