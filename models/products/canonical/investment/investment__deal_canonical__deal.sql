-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
deal_source as (
    select
        deal_id,
        deal_name,
        target_entity_id,
        strategy,
        originating_team,
        source_channel,
        structure,
        converted_record_type,
        converted_record_id
    from {{ ref("investment__deal_canonical__base") }}
)

select
    cast(deal_id as {{ dpf_type('string') }}) as deal_id,
    cast(deal_name as {{ dpf_type('string') }}) as deal_name,
    cast(target_entity_id as {{ dpf_type('string') }}) as target_entity_id,
    cast(strategy as {{ dpf_type('string') }}) as strategy,
    cast(originating_team as {{ dpf_type('string') }}) as originating_team,
    cast(source_channel as {{ dpf_type('string') }}) as source_channel,
    cast(structure as {{ dpf_type('string') }}) as structure,
    cast(converted_record_type as {{ dpf_type('string') }}) as converted_record_type,
    cast(converted_record_id as {{ dpf_type('string') }}) as converted_record_id
from deal_source
