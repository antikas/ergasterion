-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
checked_input as (
    select
        mercaro_record_id,
        mercaro_order_id,
        mercaro_product_id,
        line_number,
        quantity,
        unit_price,
        line_revenue,
        currency,
        order_date
    from {{ ref("ecommerce__mercaro_order_line_extract__checked") }}
)

select
    mercaro_record_id,
    mercaro_order_id,
    mercaro_product_id,
    line_number,
    quantity,
    unit_price,
    line_revenue,
    currency,
    order_date
from checked_input
