-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        cartivo_record_id,
        cartivo_order_id,
        cartivo_product_id,
        line_number,
        quantity,
        unit_price,
        line_revenue,
        currency,
        order_date
    from {{ ref("raw_cartivo_order_lines") }}
)

select
    cartivo_record_id,
    cartivo_order_id,
    cartivo_product_id,
    line_number,
    quantity,
    unit_price,
    line_revenue,
    currency,
    order_date,
    coalesce(cartivo_record_id is null, true) as violation_00,
    coalesce(cartivo_record_id is not null and count(*) over (partition by cartivo_record_id) > 1, true) as violation_01
from source_00
