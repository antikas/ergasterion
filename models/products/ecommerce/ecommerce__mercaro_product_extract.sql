-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
checked_input as (
    select
        mercaro_record_id,
        mercaro_product_id,
        shared_product_code,
        product_name,
        brand,
        category,
        list_price,
        currency,
        product_status,
        as_of_date
    from {{ ref("ecommerce__mercaro_product_extract__checked") }}
)

select
    mercaro_record_id,
    mercaro_product_id,
    shared_product_code,
    product_name,
    brand,
    category,
    list_price,
    currency,
    product_status,
    as_of_date
from checked_input
