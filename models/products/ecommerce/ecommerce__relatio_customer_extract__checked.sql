-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        relatio_record_id,
        relatio_customer_id,
        loyalty_id,
        email,
        full_name,
        phone,
        address_line,
        city,
        postal_code,
        country,
        customer_status,
        as_of_date
    from {{ ref("raw_relatio_customers") }}
)

select
    relatio_record_id,
    relatio_customer_id,
    loyalty_id,
    email,
    full_name,
    phone,
    address_line,
    city,
    postal_code,
    country,
    customer_status,
    as_of_date,
    coalesce(relatio_record_id is null, true) as violation_00,
    coalesce(relatio_record_id is not null and count(*) over (partition by relatio_record_id) > 1, true) as violation_01
from source_00
