-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
fact_order_line_source as (
    select
        order_id,
        line_number,
        customer_business_key,
        product_code,
        line_order_date,
        line_quantity,
        line_revenue,
        line_list_value
    from {{ ref("ecommerce__order_star__base") }}
)

select
    cast(order_id as {{ dpf_type('string') }}) as order_id,
    cast(line_number as {{ dpf_type('int') }}) as line_number,
    cast(customer_business_key as {{ dpf_type('string') }}) as customer_business_key,
    cast(product_code as {{ dpf_type('string') }}) as product_code,
    cast(line_order_date as {{ dpf_type('date') }}) as line_order_date,
    cast(line_quantity as {{ dpf_type('int') }}) as line_quantity,
    cast(line_revenue as {{ dpf_decimal_type(12, 2) }}) as line_revenue,
    cast(line_list_value as {{ dpf_decimal_type(12, 2) }}) as line_list_value
from fact_order_line_source
