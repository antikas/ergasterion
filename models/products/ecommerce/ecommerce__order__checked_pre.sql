-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        order_status,
        currency,
        channel,
        source_record_id,
        source_order_id,
        source_customer_id,
        order_date,
        order_total,
        source_system,
        order_value_band,
        customer_business_key
    from {{ ref("ecommerce__cartivo_order") }}
    union all
    select
        order_status,
        currency,
        channel,
        source_record_id,
        source_order_id,
        source_customer_id,
        order_date,
        order_total,
        source_system,
        order_value_band,
        customer_business_key
    from {{ ref("ecommerce__mercaro_order") }}
),
step_00_batch_transfer as (
    select
        order_status,
        currency,
        channel,
        source_record_id,
        source_order_id,
        source_customer_id,
        order_date,
        order_total,
        source_system,
        order_value_band,
        customer_business_key
    from source_00
)

select
    order_status,
    currency,
    channel,
    source_record_id,
    source_order_id,
    source_customer_id,
    order_date,
    order_total,
    source_system,
    order_value_band,
    customer_business_key,
    coalesce(source_record_id is null, true) as violation_00,
    coalesce(source_order_id is null, true) as violation_01
from step_00_batch_transfer
