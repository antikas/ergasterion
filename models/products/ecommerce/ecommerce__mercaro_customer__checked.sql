-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        mercaro_record_id,
        mercaro_customer_id,
        loyalty_id,
        email,
        full_name,
        city,
        buyer_segment,
        customer_status,
        as_of_date
    from {{ ref("ecommerce__mercaro_customer_extract") }}
),
step_00_batch_transfer as (
    select
        mercaro_record_id,
        mercaro_customer_id,
        loyalty_id,
        email,
        full_name,
        city,
        buyer_segment,
        customer_status,
        as_of_date
    from source_00
)

select
    mercaro_record_id,
    mercaro_customer_id,
    loyalty_id,
    email,
    full_name,
    city,
    buyer_segment,
    customer_status,
    as_of_date,
    coalesce(mercaro_record_id is null, true) as violation_00,
    coalesce(email is null, true) as violation_01
from step_00_batch_transfer
