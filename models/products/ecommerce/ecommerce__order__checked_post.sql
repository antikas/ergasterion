-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
resolution_input as (
    select
        order_status,
        order_date,
        order_total,
        order_value_band,
        customer_business_key,
        order_record_id,
        order_id,
        order_source_customer_id,
        order_source_system,
        order_currency,
        order_channel,
        customer_external_id,
        resolution_record_key,
        resolution_entity_key
    from {{ ref("ecommerce__order__resolution") }}
),
survivorship_order_status as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            order_status as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when order_status is null then 1 else 0 end, order_date desc, case when order_source_system = 'cartivo' then 1 when order_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_order_status
    where pick_rank = 1
),
survivorship_order_date as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            order_date as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when order_date is null then 1 else 0 end, order_date desc, case when order_source_system = 'cartivo' then 1 when order_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_order_date
    where pick_rank = 1
),
survivorship_order_total as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            order_total as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when order_total is null then 1 else 0 end, order_date desc, case when order_source_system = 'cartivo' then 1 when order_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_order_total
    where pick_rank = 1
),
survivorship_order_value_band as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            order_value_band as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when order_value_band is null then 1 else 0 end, order_date desc, case when order_source_system = 'cartivo' then 1 when order_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_order_value_band
    where pick_rank = 1
),
survivorship_customer_business_key as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            customer_business_key as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when customer_business_key is null then 1 else 0 end, case when order_source_system = 'cartivo' then 1 when order_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_customer_business_key
    where pick_rank = 1
),
survivorship_order_record_id as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            order_record_id as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when order_record_id is null then 1 else 0 end, order_date desc, case when order_source_system = 'cartivo' then 1 when order_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_order_record_id
    where pick_rank = 1
),
survivorship_order_id as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            order_id as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when order_id is null then 1 else 0 end, case when order_source_system = 'cartivo' then 1 when order_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_order_id
    where pick_rank = 1
),
survivorship_order_source_customer_id as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            order_source_customer_id as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when order_source_customer_id is null then 1 else 0 end, order_date desc, case when order_source_system = 'cartivo' then 1 when order_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_order_source_customer_id
    where pick_rank = 1
),
survivorship_order_source_system as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            order_source_system as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when order_source_system is null then 1 else 0 end, order_date desc, case when order_source_system = 'cartivo' then 1 when order_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_order_source_system
    where pick_rank = 1
),
survivorship_order_currency as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            order_currency as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when order_currency is null then 1 else 0 end, order_date desc, case when order_source_system = 'cartivo' then 1 when order_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_order_currency
    where pick_rank = 1
),
survivorship_order_channel as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            order_channel as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when order_channel is null then 1 else 0 end, order_date desc, case when order_source_system = 'cartivo' then 1 when order_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_order_channel
    where pick_rank = 1
),
survivorship_customer_external_id as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            customer_external_id as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when customer_external_id is null then 1 else 0 end, case when order_source_system = 'cartivo' then 1 when order_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_customer_external_id
    where pick_rank = 1
),
curation_entities as (
    select distinct
        resolution_entity_key
    from resolution_input
),
curation_survivor as (
    select
        survivorship_order_status.attribute_value as order_status,
        survivorship_order_date.attribute_value as order_date,
        survivorship_order_total.attribute_value as order_total,
        survivorship_order_value_band.attribute_value as order_value_band,
        survivorship_customer_business_key.attribute_value as customer_business_key,
        survivorship_order_record_id.attribute_value as order_record_id,
        survivorship_order_id.attribute_value as order_id,
        survivorship_order_source_customer_id.attribute_value as order_source_customer_id,
        survivorship_order_source_system.attribute_value as order_source_system,
        survivorship_order_currency.attribute_value as order_currency,
        survivorship_order_channel.attribute_value as order_channel,
        survivorship_customer_external_id.attribute_value as customer_external_id
    from curation_entities as entities
    left join survivorship_order_status
        on survivorship_order_status.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_order_date
        on survivorship_order_date.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_order_total
        on survivorship_order_total.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_order_value_band
        on survivorship_order_value_band.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_customer_business_key
        on survivorship_customer_business_key.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_order_record_id
        on survivorship_order_record_id.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_order_id
        on survivorship_order_id.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_order_source_customer_id
        on survivorship_order_source_customer_id.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_order_source_system
        on survivorship_order_source_system.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_order_currency
        on survivorship_order_currency.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_order_channel
        on survivorship_order_channel.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_customer_external_id
        on survivorship_customer_external_id.resolution_entity_key = entities.resolution_entity_key
)

select
    order_status,
    order_date,
    order_total,
    order_value_band,
    customer_business_key,
    order_record_id,
    order_id,
    order_source_customer_id,
    order_source_system,
    order_currency,
    order_channel,
    customer_external_id,
    coalesce(order_id is not null and count(*) over (partition by order_id) > 1, true) as violation_00,
    coalesce(customer_business_key is null, true) as violation_01
from curation_survivor
