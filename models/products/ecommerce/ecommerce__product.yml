# Generated by Ergasterion from a product declaration. Do not edit by hand.
version: 2
models:
- name: ecommerce__product
  description: The relation product ecommerce.product publishes as ecommerce.product.
  config:
    meta:
      dpf.product: ecommerce.product
      dpf.relation: ecommerce.product
      dpf.profile: integration
      dpf.shape: declared
      dpf.publication_mode: atomic
  data_tests:
  - dpf_contract_compliance:
      name: dpf_contract_compliance_ecommerce__product
      arguments:
        columns:
        - product_name
        - product_status
        - product_code
        - product_record_id
        - product_source_id
        - product_source_system
        - product_brand
        - product_category
        - product_list_price
        - product_currency
        - product_as_of_date
        required:
        - product_code
      config:
        severity: error
        store_failures: true
  columns:
  - name: product_name
  - name: product_status
  - name: product_code
  - name: product_record_id
  - name: product_source_id
  - name: product_source_system
  - name: product_brand
  - name: product_category
  - name: product_list_price
  - name: product_currency
  - name: product_as_of_date
- name: ecommerce__product__checked_post
  description: Every row reaching this product's validation, with the violation each declared rule computed
    for it.
  columns:
  - name: product_code
    data_tests:
    - unique:
        name: dpf_checked_post_product_code_unique
        config:
          severity: error
- name: ecommerce__product__checked_pre
  description: Every row reaching this product's validation, with the violation each declared rule computed
    for it.
  columns:
  - name: source_record_id
    data_tests:
    - not_null:
        name: dpf_checked_pre_source_record_id_not_null
        config:
          severity: error
  - name: product_code
    data_tests:
    - not_null:
        name: dpf_checked_pre_product_code_not_null
        config:
          severity: error
- name: ecommerce__product__current
  description: The current pointer onto this product's published relation.
- name: ecommerce__product__pending_keys
  description: 'The resolution evidence for this product: unmerged records, their candidate pairs, the
    match score and the review band each pair fell in.'
- name: ecommerce__product__quarantine_post
  description: One row per failing row per broken validation rule, naming the rule it broke.
- name: ecommerce__product__quarantine_pre
  description: One row per failing row per broken validation rule, naming the rule it broke.
- name: ecommerce__product__resolution
  description: Every input record with the entity its declared keys resolved it into, and the evidence
    of how.
- name: ecommerce__product__sla
  description: The publication instant and published row count of the last run.
