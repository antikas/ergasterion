-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
resolution_input as (
    select
        product_name,
        product_status,
        product_code,
        product_record_id,
        product_source_id,
        product_source_system,
        product_brand,
        product_category,
        product_list_price,
        product_currency,
        product_as_of_date,
        resolution_record_key,
        resolution_entity_key
    from {{ ref("ecommerce__product__resolution") }}
),
survivorship_product_name as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            product_name as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when product_name is null then 1 else 0 end, case when product_source_system = 'cartivo' then 1 when product_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_product_name
    where pick_rank = 1
),
survivorship_product_status as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            product_status as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when product_status is null then 1 else 0 end, product_as_of_date desc, case when product_source_system = 'cartivo' then 1 when product_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_product_status
    where pick_rank = 1
),
survivorship_product_code as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            product_code as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when product_code is null then 1 else 0 end, case when product_source_system = 'cartivo' then 1 when product_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_product_code
    where pick_rank = 1
),
survivorship_product_record_id as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            product_record_id as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when product_record_id is null then 1 else 0 end, product_as_of_date desc, case when product_source_system = 'cartivo' then 1 when product_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_product_record_id
    where pick_rank = 1
),
survivorship_product_source_id as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            product_source_id as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when product_source_id is null then 1 else 0 end, product_as_of_date desc, case when product_source_system = 'cartivo' then 1 when product_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_product_source_id
    where pick_rank = 1
),
survivorship_product_source_system as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            product_source_system as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when product_source_system is null then 1 else 0 end, product_as_of_date desc, case when product_source_system = 'cartivo' then 1 when product_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_product_source_system
    where pick_rank = 1
),
survivorship_product_brand as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            product_brand as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when product_brand is null then 1 else 0 end, case when product_source_system = 'cartivo' then 1 when product_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_product_brand
    where pick_rank = 1
),
survivorship_product_category as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            product_category as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when product_category is null then 1 else 0 end, case when product_source_system = 'cartivo' then 1 when product_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_product_category
    where pick_rank = 1
),
survivorship_product_list_price as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            product_list_price as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when product_list_price is null then 1 else 0 end, product_as_of_date desc, case when product_source_system = 'cartivo' then 1 when product_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_product_list_price
    where pick_rank = 1
),
survivorship_product_currency as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            product_currency as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when product_currency is null then 1 else 0 end, product_as_of_date desc, case when product_source_system = 'cartivo' then 1 when product_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_product_currency
    where pick_rank = 1
),
survivorship_product_as_of_date as (
    select
        resolution_entity_key,
        attribute_value
    from (
        select
            resolution_entity_key,
            product_as_of_date as attribute_value,
            row_number() over (
                partition by resolution_entity_key
                order by case when product_as_of_date is null then 1 else 0 end, product_as_of_date desc, case when product_source_system = 'cartivo' then 1 when product_source_system = 'mercaro' then 2 else 3 end, resolution_record_key
            ) as pick_rank
        from resolution_input
    ) as ranked_product_as_of_date
    where pick_rank = 1
),
curation_entities as (
    select distinct
        resolution_entity_key
    from resolution_input
),
curation_survivor as (
    select
        survivorship_product_name.attribute_value as product_name,
        survivorship_product_status.attribute_value as product_status,
        survivorship_product_code.attribute_value as product_code,
        survivorship_product_record_id.attribute_value as product_record_id,
        survivorship_product_source_id.attribute_value as product_source_id,
        survivorship_product_source_system.attribute_value as product_source_system,
        survivorship_product_brand.attribute_value as product_brand,
        survivorship_product_category.attribute_value as product_category,
        survivorship_product_list_price.attribute_value as product_list_price,
        survivorship_product_currency.attribute_value as product_currency,
        survivorship_product_as_of_date.attribute_value as product_as_of_date
    from curation_entities as entities
    left join survivorship_product_name
        on survivorship_product_name.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_product_status
        on survivorship_product_status.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_product_code
        on survivorship_product_code.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_product_record_id
        on survivorship_product_record_id.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_product_source_id
        on survivorship_product_source_id.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_product_source_system
        on survivorship_product_source_system.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_product_brand
        on survivorship_product_brand.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_product_category
        on survivorship_product_category.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_product_list_price
        on survivorship_product_list_price.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_product_currency
        on survivorship_product_currency.resolution_entity_key = entities.resolution_entity_key
    left join survivorship_product_as_of_date
        on survivorship_product_as_of_date.resolution_entity_key = entities.resolution_entity_key
)

select
    product_name,
    product_status,
    product_code,
    product_record_id,
    product_source_id,
    product_source_system,
    product_brand,
    product_category,
    product_list_price,
    product_currency,
    product_as_of_date,
    coalesce(product_code is not null and count(*) over (partition by product_code) > 1, true) as violation_00
from curation_survivor
