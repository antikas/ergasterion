-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        product_name,
        brand,
        category,
        currency,
        product_status,
        source_record_id,
        source_product_id,
        product_code,
        list_price,
        as_of_date,
        source_system
    from {{ ref("ecommerce__cartivo_product") }}
    union all
    select
        product_name,
        brand,
        category,
        currency,
        product_status,
        source_record_id,
        source_product_id,
        product_code,
        list_price,
        as_of_date,
        source_system
    from {{ ref("ecommerce__mercaro_product") }}
),
step_00_batch_transfer as (
    select
        product_name,
        brand,
        category,
        currency,
        product_status,
        source_record_id,
        source_product_id,
        product_code,
        list_price,
        as_of_date,
        source_system
    from source_00
)

select
    product_name,
    brand,
    category,
    currency,
    product_status,
    source_record_id,
    source_product_id,
    product_code,
    list_price,
    as_of_date,
    source_system,
    coalesce(source_record_id is null, true) as violation_00,
    coalesce(product_code is null, true) as violation_01
from step_00_batch_transfer
