-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        cartivo_record_id,
        cartivo_product_id,
        shared_product_code,
        product_name,
        brand,
        category,
        list_price,
        currency,
        product_status,
        as_of_date
    from {{ ref("ecommerce__cartivo_product_extract") }}
),
step_00_batch_transfer as (
    select
        cartivo_record_id,
        cartivo_product_id,
        shared_product_code,
        product_name,
        brand,
        category,
        list_price,
        currency,
        product_status,
        as_of_date
    from source_00
)

select
    cartivo_record_id,
    cartivo_product_id,
    shared_product_code,
    product_name,
    brand,
    category,
    list_price,
    currency,
    product_status,
    as_of_date,
    coalesce(cartivo_record_id is null, true) as violation_00,
    coalesce(shared_product_code is null, true) as violation_01
from step_00_batch_transfer
