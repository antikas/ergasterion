-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        mercaro_record_id,
        mercaro_order_id,
        mercaro_customer_id,
        order_date,
        order_status,
        currency,
        order_total,
        channel
    from {{ ref("ecommerce__mercaro_order_extract") }}
),
step_00_batch_transfer as (
    select
        mercaro_record_id,
        mercaro_order_id,
        mercaro_customer_id,
        order_date,
        order_status,
        currency,
        order_total,
        channel
    from source_00
)

select
    mercaro_record_id,
    mercaro_order_id,
    mercaro_customer_id,
    order_date,
    order_status,
    currency,
    order_total,
    channel,
    coalesce(mercaro_record_id is null, true) as violation_00,
    coalesce(mercaro_order_id is null, true) as violation_01
from step_00_batch_transfer
