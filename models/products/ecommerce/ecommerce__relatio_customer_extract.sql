-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
checked_input as (
    select
        relatio_record_id,
        relatio_customer_id,
        loyalty_id,
        email,
        full_name,
        phone,
        address_line,
        city,
        postal_code,
        country,
        customer_status,
        as_of_date
    from {{ ref("ecommerce__relatio_customer_extract__checked") }}
)

select
    relatio_record_id,
    relatio_customer_id,
    loyalty_id,
    email,
    full_name,
    phone,
    address_line,
    city,
    postal_code,
    country,
    customer_status,
    as_of_date
from checked_input
