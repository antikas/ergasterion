-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
checked_input as (
    select
        mercaro_record_id,
        mercaro_customer_id,
        loyalty_id,
        email,
        full_name,
        city,
        buyer_segment,
        customer_status,
        as_of_date
    from {{ ref("ecommerce__mercaro_customer_extract__checked") }}
)

select
    mercaro_record_id,
    mercaro_customer_id,
    loyalty_id,
    email,
    full_name,
    city,
    buyer_segment,
    customer_status,
    as_of_date
from checked_input
