-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
checked_input as (
    select
        cartivo_record_id,
        cartivo_order_id,
        cartivo_customer_id,
        order_date,
        order_status,
        currency,
        order_total,
        channel
    from {{ ref("ecommerce__cartivo_order_extract__checked") }}
)

select
    cartivo_record_id,
    cartivo_order_id,
    cartivo_customer_id,
    order_date,
    order_status,
    currency,
    order_total,
    channel
from checked_input
