# Generated by Ergasterion from a product declaration. Do not edit by hand.
version: 2
models:
- name: ecommerce__order
  description: The relation product ecommerce.order publishes as ecommerce.order.
  config:
    meta:
      dpf.product: ecommerce.order
      dpf.relation: ecommerce.order
      dpf.profile: integration
      dpf.shape: declared
      dpf.publication_mode: atomic
  data_tests:
  - dpf_contract_compliance:
      name: dpf_contract_compliance_ecommerce__order
      arguments:
        columns:
        - order_status
        - order_date
        - order_total
        - order_value_band
        - customer_business_key
        - order_record_id
        - order_id
        - order_source_customer_id
        - order_source_system
        - order_currency
        - order_channel
        - customer_external_id
        required:
        - customer_business_key
      config:
        severity: error
        store_failures: true
  columns:
  - name: order_status
  - name: order_date
  - name: order_total
  - name: order_value_band
  - name: customer_business_key
  - name: order_record_id
  - name: order_id
  - name: order_source_customer_id
  - name: order_source_system
  - name: order_currency
  - name: order_channel
  - name: customer_external_id
- name: ecommerce__order__checked_post
  description: Every row reaching this product's validation, with the violation each declared rule computed
    for it.
  columns:
  - name: order_id
    data_tests:
    - unique:
        name: dpf_checked_post_order_id_unique
        config:
          severity: error
  - name: customer_business_key
    data_tests:
    - not_null:
        name: dpf_checked_post_customer_business_key_not_null
        config:
          severity: error
- name: ecommerce__order__checked_pre
  description: Every row reaching this product's validation, with the violation each declared rule computed
    for it.
  columns:
  - name: source_record_id
    data_tests:
    - not_null:
        name: dpf_checked_pre_source_record_id_not_null
        config:
          severity: error
  - name: source_order_id
    data_tests:
    - not_null:
        name: dpf_checked_pre_source_order_id_not_null
        config:
          severity: error
- name: ecommerce__order__current
  description: The current pointer onto this product's published relation.
- name: ecommerce__order__pending_keys
  description: 'The resolution evidence for this product: unmerged records, their candidate pairs, the
    match score and the review band each pair fell in.'
- name: ecommerce__order__quarantine_post
  description: One row per failing row per broken validation rule, naming the rule it broke.
- name: ecommerce__order__quarantine_pre
  description: One row per failing row per broken validation rule, naming the rule it broke.
- name: ecommerce__order__resolution
  description: Every input record with the entity its declared keys resolved it into, and the evidence
    of how.
- name: ecommerce__order__sla
  description: The publication instant and published row count of the last run.
