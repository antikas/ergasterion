-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        cartivo_record_id,
        cartivo_customer_id,
        loyalty_id,
        email,
        full_name,
        phone,
        address_line,
        city,
        postal_code,
        country,
        marketing_consent,
        preferred_channel,
        customer_status,
        as_of_date
    from {{ ref("ecommerce__cartivo_customer_extract") }}
),
step_00_batch_transfer as (
    select
        cartivo_record_id,
        cartivo_customer_id,
        loyalty_id,
        email,
        full_name,
        phone,
        address_line,
        city,
        postal_code,
        country,
        marketing_consent,
        preferred_channel,
        customer_status,
        as_of_date
    from source_00
)

select
    cartivo_record_id,
    cartivo_customer_id,
    loyalty_id,
    email,
    full_name,
    phone,
    address_line,
    city,
    postal_code,
    country,
    marketing_consent,
    preferred_channel,
    customer_status,
    as_of_date,
    coalesce(cartivo_record_id is null, true) as violation_00,
    coalesce(email is null, true) as violation_01
from step_00_batch_transfer
