-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        classification_type_code,
        classification_type_name,
        description
    from {{ ref("classification_type") }}
)

select
    classification_type_code,
    classification_type_name,
    description,
    coalesce(classification_type_code is null, true) as violation_00,
    coalesce(classification_type_code is not null and count(*) over (partition by classification_type_code) > 1, true) as violation_01,
    coalesce(classification_type_name is null, true) as violation_02
from source_00
