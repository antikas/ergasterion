-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        entity_type,
        entity_external_id,
        classification_type_code,
        classification_value_code,
        effective_from,
        record_source,
        load_datetime
    from {{ ref("reference__investment_classification_history") }}
),
step_00_batch_transfer as (
    select
        entity_type,
        entity_external_id,
        classification_type_code,
        classification_value_code,
        effective_from,
        record_source,
        load_datetime
    from source_00
)

select
    entity_type,
    entity_external_id,
    classification_type_code,
    classification_value_code,
    effective_from,
    record_source,
    load_datetime,
    coalesce(entity_external_id is null, true) as violation_00,
    coalesce(effective_from is null, true) as violation_01
from step_00_batch_transfer
