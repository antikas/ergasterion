# Generated by Ergasterion from a product declaration. Do not edit by hand.
version: 2
models:
- name: reference__deal_stage_current
  description: The relation product reference.deal_stage_current publishes as reference.deal_stage_current.
  config:
    meta:
      dpf.product: reference.deal_stage_current
      dpf.relation: reference.deal_stage_current
      dpf.profile: derivation
      dpf.shape: declared
      dpf.publication_mode: atomic
  data_tests:
  - dpf_contract_compliance:
      name: dpf_contract_compliance_reference__deal_stage_current
      arguments:
        columns:
        - stage_entity_type
        - deal_external_identity
        - stage_type_code
        - stage_value_code
        - stage_effective_from
        - stage_record_source
        - stage_load_datetime
        - stage_recorded_at
        - stage_key
        - classification_value_name
        - classification_sort_order
        - stage_rank
        required:
        - deal_external_identity
        - stage_effective_from
      config:
        severity: error
        store_failures: true
  columns:
  - name: stage_entity_type
  - name: deal_external_identity
  - name: stage_type_code
  - name: stage_value_code
  - name: stage_effective_from
  - name: stage_record_source
  - name: stage_load_datetime
  - name: stage_recorded_at
  - name: stage_key
  - name: classification_value_name
  - name: classification_sort_order
  - name: stage_rank
- name: reference__deal_stage_current__checked
  description: Every row reaching this product's validation, with the violation each declared rule computed
    for it.
  columns:
  - name: deal_external_identity
    data_tests:
    - not_null:
        name: dpf_checked_deal_external_identity_not_null
        config:
          severity: error
  - name: stage_effective_from
    data_tests:
    - not_null:
        name: dpf_checked_stage_effective_from_not_null
        config:
          severity: error
- name: reference__deal_stage_current__current
  description: The current pointer onto this product's published relation.
- name: reference__deal_stage_current__filter_log
  description: One row per declared predicate, with the number of rows it excluded.
- name: reference__deal_stage_current__prefilter
  description: The relation this product filters, kept so every exclusion stays countable.
- name: reference__deal_stage_current__quarantine
  description: One row per failing row per broken validation rule, naming the rule it broke.
- name: reference__deal_stage_current__sla
  description: The publication instant and published row count of the last run.
