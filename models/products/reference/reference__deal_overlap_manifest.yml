# Generated by Ergasterion from a product declaration. Do not edit by hand.
version: 2
models:
- name: reference__deal_overlap_manifest
  description: The relation product reference.deal_overlap_manifest publishes as reference.deal_overlap_manifest.
  config:
    meta:
      dpf.product: reference.deal_overlap_manifest
      dpf.relation: reference.deal_overlap_manifest
      dpf.profile: landing
      dpf.shape: declared
      dpf.publication_mode: atomic
  data_tests:
  - dpf_contract_compliance:
      name: dpf_contract_compliance_reference__deal_overlap_manifest
      arguments:
        columns:
        - source_system
        - source_record_id
        - true_deal_external_id
        - overlap_note
        required: []
      config:
        severity: error
        store_failures: true
  columns:
  - name: source_system
  - name: source_record_id
  - name: true_deal_external_id
  - name: overlap_note
- name: reference__deal_overlap_manifest__checked
  description: Every row reaching this product's validation, with the violation each declared rule computed
    for it.
  columns:
  - name: source_record_id
    data_tests:
    - not_null:
        name: dpf_checked_source_record_id_not_null
        config:
          severity: error
    - unique:
        name: dpf_checked_source_record_id_unique
        config:
          severity: error
  - name: source_system
    data_tests:
    - not_null:
        name: dpf_checked_source_system_not_null
        config:
          severity: error
  - name: true_deal_external_id
    data_tests:
    - not_null:
        name: dpf_checked_true_deal_external_id_not_null
        config:
          severity: error
- name: reference__deal_overlap_manifest__current
  description: The current pointer onto this product's published relation.
- name: reference__deal_overlap_manifest__quarantine
  description: One row per failing row per broken validation rule, naming the rule it broke.
- name: reference__deal_overlap_manifest__sla
  description: The publication instant and published row count of the last run.
