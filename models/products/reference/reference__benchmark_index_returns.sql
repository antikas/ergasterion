-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
checked_input as (
    select
        benchmark_id,
        benchmark_name,
        index_date,
        index_value,
        currency,
        source_system
    from {{ ref("reference__benchmark_index_returns__checked") }}
)

select
    benchmark_id,
    benchmark_name,
    index_date,
    index_value,
    currency,
    source_system
from checked_input
