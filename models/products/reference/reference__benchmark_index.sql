-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
checked_input as (
    select
        benchmark_id,
        benchmark_name,
        index_date,
        index_value,
        currency,
        source_system
    from {{ ref("reference__benchmark_index__checked") }}
),
step_02_schema_transform as (
    select
        cast(benchmark_id as {{ dpf_type('string') }}) as benchmark_id,
        cast(benchmark_name as {{ dpf_type('string') }}) as benchmark_name,
        cast(index_date as {{ dpf_type('date') }}) as benchmark_on,
        cast(index_value as {{ dpf_decimal_type(18, 4) }}) as benchmark_index_value,
        cast(currency as {{ dpf_type('string') }}) as benchmark_currency
    from checked_input
),
step_03_calculated_fields as (
    select
        benchmark_id,
        benchmark_name,
        benchmark_on,
        benchmark_index_value,
        benchmark_currency,
        cast((concat(benchmark_id, ' (', benchmark_currency, ')')) as {{ dpf_type('string') }}) as benchmark_label
    from step_02_schema_transform
)

select
    benchmark_id,
    benchmark_name,
    benchmark_on,
    benchmark_index_value,
    benchmark_currency,
    benchmark_label
from step_03_calculated_fields
