-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        benchmark_id,
        benchmark_name,
        index_date,
        index_value,
        currency,
        source_system
    from {{ ref("reference__benchmark_index_returns") }}
),
step_00_batch_transfer as (
    select
        benchmark_id,
        benchmark_name,
        index_date,
        index_value,
        currency,
        source_system
    from source_00
)

select
    benchmark_id,
    benchmark_name,
    index_date,
    index_value,
    currency,
    source_system,
    coalesce(benchmark_id is null, true) as violation_00,
    coalesce(index_date is null, true) as violation_01
from step_00_batch_transfer
