-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='table') }}

with
checked_input as (
    select
        source_system,
        source_record_id,
        true_deal_external_id,
        overlap_note
    from {{ ref("reference__deal_overlap_manifest__checked") }}
)

select
    source_system,
    source_record_id,
    true_deal_external_id,
    overlap_note
from checked_input
