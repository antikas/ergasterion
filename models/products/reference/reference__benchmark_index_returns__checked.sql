-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        benchmark_id,
        benchmark_name,
        index_date,
        index_value,
        currency,
        source_system
    from {{ ref("benchmark_index_returns") }}
)

select
    benchmark_id,
    benchmark_name,
    index_date,
    index_value,
    currency,
    source_system,
    coalesce(benchmark_id is null, true) as violation_00,
    coalesce(index_date is null, true) as violation_01,
    coalesce(index_value is null, true) as violation_02
from source_00
