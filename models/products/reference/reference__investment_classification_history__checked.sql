-- Generated by Ergasterion from a product declaration. Do not edit by hand.
{{ config(materialized='view') }}

with
source_00 as (
    select
        entity_type,
        entity_external_id,
        classification_type_code,
        classification_value_code,
        effective_from,
        record_source,
        load_datetime
    from {{ ref("investment_classification_history_seed") }}
)

select
    entity_type,
    entity_external_id,
    classification_type_code,
    classification_value_code,
    effective_from,
    record_source,
    load_datetime,
    coalesce(entity_external_id is null, true) as violation_00,
    coalesce(classification_type_code is null, true) as violation_01,
    coalesce(effective_from is null, true) as violation_02
from source_00
