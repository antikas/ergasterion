-- Generated by ergasterion/emit.py from declarations/*.yml. Do not edit by hand.
with source as (
    select * from {{ ref('raw_vantora_portfolio_companies') }}
)

select
    'vantora' as source_system,
    cast(vantora_company_id as string) as source_company_id,
    cast(vantora_fund_id as string) as source_fund_id,
    cast(company_name as string) as company_name,
    {{ blank_to_null('company_lei') }} as lei,
    {{ blank_to_null('shared_external_id') }} as shared_external_id,
    cast(sector as string) as sector,
    cast(sub_sector as string) as sub_sector,
    cast(country as string) as country,
    cast(status as string) as status,
    {{ dpf_safe_cast('first_seen_at', 'date') }} as first_seen_at
from source
