-- Generated by ergasterion/emit.py from declarations/*.yml. Do not edit by hand.
with source as (
    select * from {{ ref('raw_chrono_portfolio_companies') }}
)

select
    'chrono' as source_system,
    cast(asset_uid as string) as source_company_id,
    cast(vehicle_code as string) as source_fund_id,
    cast(issuer_name_text as string) as company_name,
    {{ blank_to_null('issuer_lei') }} as lei,
    {{ blank_to_null('vendor_company_id') }} as shared_external_id,
    cast(industry_group as string) as sector,
    cast(industry_detail as string) as sub_sector,
    cast(country_iso2 as string) as country,
    cast(holding_status as string) as status,
    {{ dpf_safe_cast('first_reported_on', 'date') }} as first_seen_at
from source
