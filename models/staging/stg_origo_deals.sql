-- Generated by ergasterion/emit.py from declarations/*.yml. Do not edit by hand.
with source as (
    select * from {{ ref('raw_origo_deals') }}
)

select
    'origo' as source_system,
    cast(deal_id as string) as source_record_id,
    cast(deal_id as string) as source_deal_id,
    {{ blank_to_null('external_deal_id') }} as external_deal_id,
    cast(deal_name as string) as deal_name,
    {{ blank_to_null('target_entity_id') }} as target_entity_id,
    cast(strategy as string) as strategy,
    cast(originating_team as string) as originating_team,
    cast(source_channel as string) as source_channel,
    {{ dpf_safe_cast('sourced_date', 'date') }} as sourced_date,
    cast(structure as string) as structure,
    {{ blank_to_null('converted_record_type') }} as converted_record_type,
    {{ blank_to_null('converted_record_id') }} as converted_record_id
from source
