-- Generated by ergasterion/emit.py from declarations/*.yml. Do not edit by hand.
with source as (
    select * from {{ ref('raw_cartivo_order_lines') }}
)

select
    'cartivo' as source_system,
    cast(cartivo_record_id as string) as source_record_id,
    cast(cartivo_order_id as string) as source_order_id,
    cast(cartivo_product_id as string) as source_product_id,
    {{ dpf_safe_cast('line_number', 'int') }} as line_number,
    {{ dpf_safe_cast('quantity', 'int') }} as quantity,
    {{ dpf_safe_cast('unit_price', 'numeric') }} as unit_price,
    {{ dpf_safe_cast('line_revenue', 'numeric') }} as line_revenue,
    cast(currency as string) as currency,
    {{ dpf_safe_cast('order_date', 'date') }} as order_date
from source
