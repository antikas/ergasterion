-- Generated by ergasterion/emit.py from declarations/*.yml. Do not edit by hand.
with source as (
    select * from {{ ref('raw_vantora_funds') }}
)

select
    'vantora' as source_system,
    cast(vantora_record_id as string) as source_record_id,
    cast(vantora_fund_id as string) as source_fund_id,
    cast(fund_name as string) as fund_name,
    cast(fund_family_name as string) as fund_family_name,
    cast(vehicle_type as string) as vehicle_type,
    cast(gp_source_id as string) as source_gp_id,
    cast(gp_name as string) as gp_name,
    {{ blank_to_null('fund_lei') }} as lei,
    {{ blank_to_null('shared_external_id') }} as shared_external_id,
    cast(asset_class as string) as asset_class,
    cast(strategy as string) as strategy,
    {{ dpf_safe_cast('vintage_year', 'int') }} as vintage_year,
    {{ dpf_safe_cast('committed_capital_usd', 'numeric') }} as committed_capital_usd,
    cast(currency as string) as currency,
    cast(domicile as string) as domicile,
    cast(fund_status as string) as fund_status,
    {{ dpf_safe_cast('as_of_date', 'date') }} as as_of_date
from source
