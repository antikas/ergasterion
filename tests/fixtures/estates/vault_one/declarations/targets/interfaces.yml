# This estate's declared interface boundaries: the model paths whose
# artefacts may materialise as views. It declares none. A vault computes
# and stores; it is never an interface boundary, so every relation this
# estate's products publish materialises as a table or as an insert-only
# store, and the structural budget still governs the views the translator
# renders privately under a product's namespace.
view_layers: []
