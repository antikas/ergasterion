# Generated by `ergasterion init`: the engine-generic structural top only.
# Add your own `models:` overrides as you introduce layers this estate doesn't have,
# and a `seeds:` block once you add a seed -- seed `column_types` are authored
# config with no generator (see GETTING-STARTED.md).

name: ergasterion
version: 0.1.0
config-version: 2
profile: ergasterion
model-paths:
- models
seed-paths:
- seeds
test-paths:
- tests
macro-paths:
- macros
analysis-paths:
- analyses
snapshot-paths:
- snapshots
target-path: runtime/data/dbt/target
log-path: runtime/data/dbt/logs
packages-install-path: runtime/data/dbt/packages
clean-targets:
- runtime/data/dbt/target
- runtime/data/dbt/packages
- runtime/data/dbt/logs
models:
  ergasterion:
    +materialized: table
