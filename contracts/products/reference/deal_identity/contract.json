{
  "access": {
    "classification": "internal"
  },
  "description": "The deal identity each source system record belongs to, keyed by the system and record together.",
  "freshness": "daily by 06:00 UTC",
  "identity": {
    "domain": "reference",
    "major": 1,
    "name": "deal_identity",
    "namespace": "io.antikas.ergasterion"
  },
  "lineage": [
    "reference.deal_overlap_manifest@1"
  ],
  "quality": [
    {
      "description": "source_record_id must not be null",
      "dimension": "completeness"
    },
    {
      "description": "true_deal_external_id must not be null",
      "dimension": "completeness"
    }
  ],
  "relations": [
    {
      "fields": [
        {
          "name": "identity_source_system",
          "required": false,
          "type": "string"
        },
        {
          "name": "identity_source_record_id",
          "required": false,
          "type": "string"
        },
        {
          "name": "deal_external_identity",
          "required": false,
          "type": "string"
        },
        {
          "name": "deal_identity_note",
          "required": false,
          "type": "string"
        },
        {
          "name": "deal_identity_key",
          "required": false,
          "type": "string"
        }
      ],
      "name": "reference.deal_identity"
    }
  ],
  "schema": "ergasterion.product-contract/v1",
  "support": "platform-support",
  "team": "data-product-factory",
  "versioningPolicy": "additive nullable fields are minor and non-breaking; removals, renames, type changes and new required fields are major and breaking"
}
