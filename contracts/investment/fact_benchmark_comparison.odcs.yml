# Generated by ergasterion/emit_contracts.py from the dbt manifest + declarations/domains SSOT.
# ODCS v3.1.0 data contract -- regenerated, never hand-edited. Do not edit by hand.
apiVersion: v3.1.0
kind: DataContract
id: dpf:investment:fact_benchmark_comparison
version: 1.0.0
status: active
name: fact_benchmark_comparison
domain: investment
description:
  purpose: 'Benchmark-relative metrics of the catalogue: pme (Kaplan-Schoar public-market equivalent),
    alpha_bps (annualised net_irr excess over the benchmark, in basis points), and quartile_rank (NTILE(4)
    within the vintage x strategy peer cohort by net_irr). The benchmark index join is evaluated from
    the declared dbt relations at query time. Every ER-confirmed fund gets a row here even when its performance_as_of_date
    falls outside the seeded benchmark-index window (left join, not inner) -- coverage is asserted by
    tests/assert_benchmark_comparison_confirmed_fund_coverage.sql.'
servers:
- server: ergasterion_snowflake
  type: snowflake
  environment: production
  account: ergasterion
  database: ERGASTERION
  schema: MARTS
schema:
- name: fact_benchmark_comparison
  physicalName: fact_benchmark_comparison
  logicalType: object
  physicalType: table
  properties:
  - name: benchmark_comparison_key
    logicalType: string
    primaryKey: true
    primaryKeyPosition: 1
    required: true
    unique: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: library
      metric: duplicateValues
      mustBe: 0
      dimension: uniqueness
      description: 'dbt unique test: no duplicate values.'
  - name: fund_key
    logicalType: string
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: custom
      engine: dbt
      implementation: 'dbt relationships test: every value exists in dim_fund.fund_key'
      dimension: consistency
      description: Referential-integrity check against the parent served table.
  - name: date_key
    logicalType: string
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: custom
      engine: dbt
      implementation: 'dbt relationships test: every value exists in dim_date.date_key'
      dimension: consistency
      description: Referential-integrity check against the parent served table.
  - name: benchmark_id
    logicalType: string
    description: ': NULL when the fund''s performance_as_of_date falls outside the seeded benchmark-index
      window -- an honest absence-of-coverage signal, not an error, so this column intentionally carries
      no not_null test. pme/alpha_bps are NULL for the same rows; quartile_rank still populates (benchmark-independent).'
  - name: pme
    logicalType: number
    description: Kaplan-Schoar PME; > 1 indicates outperformance of the public-market benchmark.
    quality:
    - type: custom
      engine: dbt
      implementation: 'dbt_utils accepted_range test: {"inclusive": true, "min_value": 0}'
      dimension: conformity
      description: Numeric range constraint on the column.
  - name: quartile_rank
    logicalType: number
    description: Peer-cohort quartile (1 = top) within vintage_year x strategy by net_irr.
    quality:
    - type: custom
      engine: dbt
      implementation: 'dbt_utils accepted_range test: {"inclusive": true, "max_value": 4, "min_value":
        1}'
      dimension: conformity
      description: Numeric range constraint on the column.
  quality:
  - type: custom
    engine: dbt
    implementation: 'dbt singular data test: assert_benchmark_comparison_confirmed_fund_coverage'
    dimension: consistency
    description: Bespoke table-level dbt assertion.
authoritativeDefinitions:
- url: https://openinvestmentmodel.org
  type: businessDefinition
