# Generated by ergasterion/emit_contracts.py from the dbt manifest + declarations/domains SSOT.
# ODCS v3.1.0 data contract -- regenerated, never hand-edited. Do not edit by hand.
apiVersion: v3.1.0
kind: DataContract
id: dpf:investment:fact_fund_performance
version: 1.0.0
status: active
name: fact_fund_performance
domain: investment
description:
  purpose: 'Fund performance fact: the nine fund-intrinsic metrics of the 12-metric catalogue -- tvpi,
    dpi, rvpi, moic, nav, called_pct, unfunded_commitment, net_irr, gross_irr. net_irr is a real deterministic
    XIRR solve (macros/xirr.sql), and gross_irr uses the same declared flow basis. See the fact_fund_performance_base
    description. Only ER-confirmed golden funds (dpf_is_er_confirmed) feed the calculation. tvpi/dpi/rvpi/moic
    are declarative calculated_field outputs (cf_fund_multiples.sql), joined back in from fact_fund_performance_base
    -- this mart''s public columns and grain are unchanged.'
servers:
- server: ergasterion_snowflake
  type: snowflake
  environment: production
  account: ergasterion
  database: ERGASTERION
  schema: MARTS
schema:
- name: fact_fund_performance
  physicalName: fact_fund_performance
  logicalType: object
  physicalType: table
  properties:
  - name: fund_performance_key
    logicalType: string
    primaryKey: true
    primaryKeyPosition: 1
    required: true
    unique: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: library
      metric: duplicateValues
      mustBe: 0
      dimension: uniqueness
      description: 'dbt unique test: no duplicate values.'
  - name: fund_key
    logicalType: string
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: custom
      engine: dbt
      implementation: 'dbt relationships test: every value exists in dim_fund.fund_key'
      dimension: consistency
      description: Referential-integrity check against the parent served table.
  - name: tvpi
    logicalType: number
    description: Total value to paid-in = (distributions + NAV) / paid-in. Reconciles to dpi + rvpi.
    quality:
    - type: custom
      engine: dbt
      implementation: 'dbt_utils accepted_range test: {"inclusive": true, "min_value": 0}'
      dimension: conformity
      description: Numeric range constraint on the column.
  - name: dpi
    logicalType: number
    description: Distributions to paid-in.
    quality:
    - type: custom
      engine: dbt
      implementation: 'dbt_utils accepted_range test: {"inclusive": true, "min_value": 0}'
      dimension: conformity
      description: Numeric range constraint on the column.
  - name: rvpi
    logicalType: number
    description: Residual value (NAV) to paid-in.
    quality:
    - type: custom
      engine: dbt
      implementation: 'dbt_utils accepted_range test: {"inclusive": true, "min_value": 0}'
      dimension: conformity
      description: Numeric range constraint on the column.
  - name: moic
    logicalType: number
    description: Multiple on invested capital = (distributions + NAV) / invested capital, where invested
      capital is paid-in less recallable distributions and fee offsets. Distinct from tvpi (which divides
      by paid-in) and >= tvpi because invested capital <= paid-in; equal to tvpi only for funds with no
      seeded invested basis.
    quality:
    - type: custom
      engine: dbt
      implementation: 'dbt_utils accepted_range test: {"inclusive": true, "min_value": 0}'
      dimension: conformity
      description: Numeric range constraint on the column.
  - name: called_pct
    logicalType: number
    description: Paid-in capital as a fraction of committed capital.
    quality:
    - type: custom
      engine: dbt
      implementation: 'dbt_utils accepted_range test: {"inclusive": true, "min_value": 0}'
      dimension: conformity
      description: Numeric range constraint on the column.
  - name: net_irr
    logicalType: string
    description: Net internal rate of return from a deterministic XIRR solve (macros/xirr.sql via int_fund_xirr)
      over net LP cash flows plus terminal NAV. NULL where the stream is not bracketed by a root (no NPV
      sign change across the solver bracket), rather than a fabricated 0 or a ceiling-pinned rate.
  - name: gross_irr
    logicalType: string
    description: Uses the net_irr result because the canonical cash-flow schema declares no separate fee/carry
      event subtype or distinct gross flow basis. Inherits net_irr's NULL-when-unbracketed semantics.
  quality:
  - type: custom
    engine: dbt
    implementation: 'dbt singular data test: assert_apex_tvpi_dpi_expected_values'
    dimension: consistency
    description: Bespoke table-level dbt assertion.
  - type: custom
    engine: dbt
    implementation: 'dbt singular data test: assert_benchmark_comparison_confirmed_fund_coverage'
    dimension: consistency
    description: Bespoke table-level dbt assertion.
  - type: custom
    engine: dbt
    implementation: 'dbt singular data test: assert_fund_net_irr_expected_value'
    dimension: consistency
    description: Bespoke table-level dbt assertion.
authoritativeDefinitions:
- url: https://openinvestmentmodel.org
  type: businessDefinition
