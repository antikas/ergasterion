# Generated by ergasterion/emit_contracts.py from the dbt manifest + declarations/domains SSOT.
# ODCS v3.1.0 data contract -- regenerated, never hand-edited. Do not edit by hand.
apiVersion: v3.1.0
kind: DataContract
id: dpf:investment:dim_deal_stage
version: 1.0.0
status: active
name: dim_deal_stage
domain: investment
description:
  purpose: 'SCD2 deal-stage-history dimension using the reusable classification pattern (classification_type_code
    = DEAL_STAGE) but keyed directly on golden_deal_key -- deals are single-source ORIGO with their own
    golden keys, so the fund satellite''s known shared_external_id-orphan limitation (docs/architecture/scd2-classification.md)
    does not apply. Half-open ranges: effective_from <= event date < effective_to.'
servers:
- server: ergasterion_snowflake
  type: snowflake
  environment: production
  account: ergasterion
  database: ERGASTERION
  schema: MARTS
schema:
- name: dim_deal_stage
  physicalName: dim_deal_stage
  logicalType: object
  physicalType: table
  properties:
  - name: deal_stage_key
    logicalType: string
    primaryKey: true
    primaryKeyPosition: 1
    required: true
    unique: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: library
      metric: duplicateValues
      mustBe: 0
      dimension: uniqueness
      description: 'dbt unique test: no duplicate values.'
  - name: deal_id
    logicalType: string
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
  - name: stage_code
    logicalType: string
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
  - name: effective_from
    logicalType: date
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
  - name: stage_recorded_at
    logicalType: date
    description: 'Intra-day ordering column: midnight of effective_from for a seed-sourced row, the approvals
      decision''s own decided_at timestamp for a decision-derived row. The SCD2 window above orders on
      (effective_from, stage_recorded_at, deal_stage_history_key) so a same-day stage transition resolves
      deterministically instead of falling back to the surrogate key. Null only for the reserved UNKNOWN
      member.'
  - name: effective_to
    logicalType: date
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
  quality:
  - type: custom
    engine: dbt
    implementation: 'dbt singular data test: assert_deal_approval_produces_next_stage'
    dimension: consistency
    description: Bespoke table-level dbt assertion.
  - type: custom
    engine: dbt
    implementation: 'dbt singular data test: assert_deal_stage_point_in_time'
    dimension: consistency
    description: Bespoke table-level dbt assertion.
  - type: custom
    engine: dbt
    implementation: 'dbt singular data test: assert_deal_stage_same_day_determinism'
    dimension: consistency
    description: Bespoke table-level dbt assertion.
authoritativeDefinitions:
- url: https://openinvestmentmodel.org
  type: businessDefinition
