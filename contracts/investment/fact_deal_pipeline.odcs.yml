# Generated by ergasterion/emit_contracts.py from the dbt manifest + declarations/domains SSOT.
# ODCS v3.1.0 data contract -- regenerated, never hand-edited. Do not edit by hand.
apiVersion: v3.1.0
kind: DataContract
id: dpf:investment:fact_deal_pipeline
version: 1.0.0
status: active
name: fact_deal_pipeline
domain: investment
description:
  purpose: 'Deal-pipeline funnel snapshot: one row per resolved deal, its stage as of today via a half-open
    date-range join against dim_deal_stage, conversion linkage to a resolved fund golden key, and cycle-time
    measures (days in the current stage; total sourced-to-decision cycle time for deals that reached a
    terminal COMMITTED/DECLINED stage).'
servers:
- server: ergasterion_snowflake
  type: snowflake
  environment: production
  account: ergasterion
  database: ERGASTERION
  schema: MARTS
schema:
- name: fact_deal_pipeline
  physicalName: fact_deal_pipeline
  logicalType: object
  physicalType: table
  properties:
  - name: deal_pipeline_key
    logicalType: string
    primaryKey: true
    primaryKeyPosition: 1
    required: true
    unique: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: library
      metric: duplicateValues
      mustBe: 0
      dimension: uniqueness
      description: 'dbt unique test: no duplicate values.'
  - name: deal_key
    logicalType: string
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: custom
      engine: dbt
      implementation: 'dbt relationships test: every value exists in dim_deal.deal_key'
      dimension: consistency
      description: Referential-integrity check against the parent served table.
  - name: date_key
    logicalType: string
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: custom
      engine: dbt
      implementation: 'dbt relationships test: every value exists in dim_date.date_key'
      dimension: consistency
      description: Referential-integrity check against the parent served table.
  - name: current_deal_stage_key
    logicalType: string
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: custom
      engine: dbt
      implementation: 'dbt relationships test: every value exists in dim_deal_stage.deal_stage_key'
      dimension: consistency
      description: Referential-integrity check against the parent served table.
  - name: converted_fund_id
    logicalType: string
    quality:
    - type: custom
      engine: dbt
      implementation: 'dbt relationships test: every value exists in dim_fund.fund_id'
      dimension: consistency
      description: Referential-integrity check against the parent served table.
  quality:
  - type: custom
    engine: dbt
    implementation: 'dbt singular data test: assert_deal_conversion_links_to_fund_golden_key'
    dimension: consistency
    description: Bespoke table-level dbt assertion.
  - type: custom
    engine: dbt
    implementation: 'dbt singular data test: assert_pipeline_covers_all_golden_deals'
    dimension: consistency
    description: Bespoke table-level dbt assertion.
authoritativeDefinitions:
- url: https://openinvestmentmodel.org
  type: businessDefinition
