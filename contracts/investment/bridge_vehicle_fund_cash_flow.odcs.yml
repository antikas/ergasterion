# Generated by ergasterion/emit_contracts.py from the dbt manifest + declarations/domains SSOT.
# ODCS v3.1.0 data contract -- regenerated, never hand-edited. Do not edit by hand.
apiVersion: v3.1.0
kind: DataContract
id: dpf:investment:bridge_vehicle_fund_cash_flow
version: 1.0.0
status: active
name: bridge_vehicle_fund_cash_flow
domain: investment
description:
  purpose: 'Vehicle-to-fund cash-flow aggregation bridge: vehicle-grain cash flows rolled up to fund grain
    beside the fund''s own fund-grain total, with the reconciliation delta. One row per fund that has
    vehicles. The named test assert_vehicle_to_fund_cash_flow_conservation asserts the delta stays within
    epsilon.'
servers:
- server: ergasterion_snowflake
  type: snowflake
  environment: production
  account: ergasterion
  database: ERGASTERION
  schema: MARTS
schema:
- name: bridge_vehicle_fund_cash_flow
  physicalName: bridge_vehicle_fund_cash_flow
  logicalType: object
  physicalType: table
  properties:
  - name: fund_id
    logicalType: string
    primaryKey: true
    primaryKeyPosition: 1
    required: true
    unique: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: custom
      engine: dbt
      implementation: 'dbt relationships test: every value exists in dim_fund.fund_id'
      dimension: consistency
      description: Referential-integrity check against the parent served table.
    - type: library
      metric: duplicateValues
      mustBe: 0
      dimension: uniqueness
      description: 'dbt unique test: no duplicate values.'
  quality:
  - type: custom
    engine: dbt
    implementation: 'dbt singular data test: assert_no_orphan_vehicle_cash_flows'
    dimension: consistency
    description: Bespoke table-level dbt assertion.
  - type: custom
    engine: dbt
    implementation: 'dbt singular data test: assert_vehicle_to_fund_cash_flow_conservation'
    dimension: consistency
    description: Bespoke table-level dbt assertion.
authoritativeDefinitions:
- url: https://openinvestmentmodel.org
  type: businessDefinition
