# Generated by ergasterion/emit_contracts.py from the dbt manifest + declarations/domains SSOT.
# ODCS v3.1.0 data contract -- regenerated, never hand-edited. Do not edit by hand.
apiVersion: v3.1.0
kind: DataContract
id: dpf:investment:dim_gp
version: 1.0.0
status: active
name: dim_gp
domain: investment
description:
  purpose: 'GP / management-company dimension keyed by OpenIM golden GP ID. Extended with manager-succession
    lineage columns: predecessor_gp_key / successor_gp_key are self-references into this same dimension;
    lineage_status distinguishes a succession predecessor (''superseded''), a succession successor (''current''),
    and every other GP (''active'').'
servers:
- server: ergasterion_snowflake
  type: snowflake
  environment: production
  account: ergasterion
  database: ERGASTERION
  schema: MARTS
schema:
- name: dim_gp
  physicalName: dim_gp
  logicalType: object
  physicalType: table
  properties:
  - name: gp_key
    logicalType: string
    primaryKey: true
    primaryKeyPosition: 1
    required: true
    unique: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: library
      metric: duplicateValues
      mustBe: 0
      dimension: uniqueness
      description: 'dbt unique test: no duplicate values.'
  - name: gp_id
    logicalType: string
    required: true
    unique: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: library
      metric: duplicateValues
      mustBe: 0
      dimension: uniqueness
      description: 'dbt unique test: no duplicate values.'
  - name: predecessor_gp_key
    logicalType: string
    quality:
    - type: custom
      engine: dbt
      implementation: 'dbt relationships test: every value exists in dim_gp.gp_key'
      dimension: consistency
      description: Referential-integrity check against the parent served table.
  - name: successor_gp_key
    logicalType: string
    quality:
    - type: custom
      engine: dbt
      implementation: 'dbt relationships test: every value exists in dim_gp.gp_key'
      dimension: consistency
      description: Referential-integrity check against the parent served table.
  - name: lineage_status
    logicalType: string
    quality:
    - type: custom
      engine: dbt
      implementation: 'dbt accepted_values test: value in ["active", "superseded", "current"]'
      dimension: conformity
      description: Domain/enumeration constraint on the column.
  quality:
  - type: custom
    engine: dbt
    implementation: 'dbt singular data test: assert_gp_succession_continuity'
    dimension: consistency
    description: Bespoke table-level dbt assertion.
authoritativeDefinitions:
- url: https://openinvestmentmodel.org
  type: businessDefinition
customProperties:
- property: dpf.survivorship.entity
  value: gp
- property: dpf.survivorship.sourcePriority
  value:
  - VANTORA
  - MERIDEX
  - PORTIQ
  - CHRONO
- property: dpf.survivorship.rules
  value:
    source_gp_id: first_non_null
    source_fund_id: first_non_null
    gp_name: first_non_null
    lei: first_non_null
    shared_external_id: first_non_null
    domicile: first_non_null
    relationship_start_date: most_recent
    gp_resolution_tier: first_non_null
    gp_resolution_confidence: first_non_null
