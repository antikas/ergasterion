# Generated by ergasterion/emit_contracts.py from the dbt manifest + declarations/domains SSOT.
# ODCS v3.1.0 data contract -- regenerated, never hand-edited. Do not edit by hand.
apiVersion: v3.1.0
kind: DataContract
id: dpf:investment:dim_fund_gp_attribution
version: 1.0.0
status: active
name: dim_fund_gp_attribution
domain: investment
description:
  purpose: 'Fund-to-GP attribution as of a date: joins link_gp_succession''s continuity claim into the
    dim_investment_classification half-open date-range pattern, so "which GP does this fund attribute
    to on date D" is a query against this model (fund_id + effective_from <= date < effective_to) instead
    of a test-side recomputation. One row per fund per attribution window; a fund untouched by any succession
    event carries exactly one row spanning 1900-01-01 to 9999-12-31.'
servers:
- server: ergasterion_snowflake
  type: snowflake
  environment: production
  account: ergasterion
  database: ERGASTERION
  schema: MARTS
schema:
- name: dim_fund_gp_attribution
  physicalName: dim_fund_gp_attribution
  logicalType: object
  physicalType: table
  properties:
  - name: fund_gp_attribution_key
    logicalType: string
    primaryKey: true
    primaryKeyPosition: 1
    required: true
    unique: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: library
      metric: duplicateValues
      mustBe: 0
      dimension: uniqueness
      description: 'dbt unique test: no duplicate values.'
  - name: fund_id
    logicalType: string
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: custom
      engine: dbt
      implementation: 'dbt relationships test: every value exists in dim_fund.fund_id'
      dimension: consistency
      description: Referential-integrity check against the parent served table.
  - name: gp_id
    logicalType: string
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: custom
      engine: dbt
      implementation: 'dbt relationships test: every value exists in dim_gp.gp_id'
      dimension: consistency
      description: Referential-integrity check against the parent served table.
  - name: attribution_source_type
    logicalType: string
    quality:
    - type: custom
      engine: dbt
      implementation: 'dbt accepted_values test: value in ["fund_source_record", "gp_succession_event"]'
      dimension: conformity
      description: Domain/enumeration constraint on the column.
  - name: effective_from
    logicalType: date
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
  - name: effective_to
    logicalType: date
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
  quality:
  - type: custom
    engine: dbt
    implementation: 'dbt singular data test: assert_gp_succession_continuity'
    dimension: consistency
    description: Bespoke table-level dbt assertion.
authoritativeDefinitions:
- url: https://openinvestmentmodel.org
  type: businessDefinition
