# Generated by ergasterion/emit_contracts.py from the dbt manifest + declarations/domains SSOT.
# ODCS v3.1.0 data contract -- regenerated, never hand-edited. Do not edit by hand.
apiVersion: v3.1.0
kind: DataContract
id: dpf:ecommerce:dim_customer_segment
version: 1.0.0
status: active
name: dim_customer_segment
domain: ecommerce
description:
  purpose: 'SCD2 customer segment (loyalty-tier) dimension, the e-commerce domain''s instance of the reusable
    half-open-range SCD2 pattern (docs/architecture/scd2-classification.md), over the seeded segment/loyalty-tier
    history staged. Half-open ranges: effective_from <= event date < effective_to. Named test: assert_customer_segment_point_in_time.sql
    (point-in-time attribution, no gap, no overlap).'
servers:
- server: ergasterion_snowflake
  type: snowflake
  environment: production
  account: ergasterion
  database: ERGASTERION
  schema: MARTS
schema:
- name: dim_customer_segment
  physicalName: dim_customer_segment
  logicalType: object
  physicalType: table
  properties:
  - name: customer_segment_key
    logicalType: string
    primaryKey: true
    primaryKeyPosition: 1
    required: true
    unique: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: library
      metric: duplicateValues
      mustBe: 0
      dimension: uniqueness
      description: 'dbt unique test: no duplicate values.'
  - name: customer_id
    logicalType: string
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: custom
      engine: dbt
      implementation: 'dbt relationships test: every value exists in dim_customer.customer_id'
      dimension: consistency
      description: Referential-integrity check against the parent served table.
  - name: segment_code
    logicalType: string
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
  - name: effective_from
    logicalType: date
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
  - name: effective_to
    logicalType: date
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
  - name: segment_recorded_at
    logicalType: date
    description: 'Intra-day ordering column (precedent, applied defensively): the SCD2 window above orders
      on (effective_from, segment_recorded_at, customer_segment_history_key) so a same-day segment transition
      would resolve deterministically instead of falling back to the surrogate key -- see sat_customer_segment_history.sql''s
      header.'
  quality:
  - type: custom
    engine: dbt
    implementation: 'dbt singular data test: assert_customer_segment_point_in_time'
    dimension: consistency
    description: Bespoke table-level dbt assertion.
