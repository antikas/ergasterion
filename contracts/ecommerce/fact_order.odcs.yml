# Generated by ergasterion/emit_contracts.py from the dbt manifest + declarations/domains SSOT.
# ODCS v3.1.0 data contract -- regenerated, never hand-edited. Do not edit by hand.
apiVersion: v3.1.0
kind: DataContract
id: dpf:ecommerce:fact_order
version: 1.0.0
status: active
name: fact_order
domain: ecommerce
description:
  purpose: 'E-commerce order-line fact, the domain''s conserved-measure grain: line_revenue summed per
    order reconciles to that order''s header total (int_order_header.order_total) within epsilon -- the
    assert_bridge_identity pattern applied here as assert_order_line_revenue_conservation.sql. Every order
    links to a real customer golden key, asserted by the customer_id relationships test below (the "every
    order links to a real customer golden key" named test).'
servers:
- server: ergasterion_snowflake
  type: snowflake
  environment: production
  account: ergasterion
  database: ERGASTERION
  schema: MARTS
schema:
- name: fact_order
  physicalName: fact_order
  logicalType: object
  physicalType: table
  properties:
  - name: order_line_key
    logicalType: string
    primaryKey: true
    primaryKeyPosition: 1
    required: true
    unique: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: library
      metric: duplicateValues
      mustBe: 0
      dimension: uniqueness
      description: 'dbt unique test: no duplicate values.'
  - name: order_id
    logicalType: string
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
  - name: customer_id
    logicalType: string
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: custom
      engine: dbt
      implementation: 'dbt relationships test: every value exists in dim_customer.customer_id'
      dimension: consistency
      description: Referential-integrity check against the parent served table.
  - name: product_id
    logicalType: string
    required: true
    quality:
    - type: library
      metric: nullValues
      mustBe: 0
      dimension: completeness
      description: 'dbt not_null test: no null values.'
    - type: custom
      engine: dbt
      implementation: 'dbt relationships test: every value exists in dim_product.product_id'
      dimension: consistency
      description: Referential-integrity check against the parent served table.
  quality:
  - type: custom
    engine: dbt
    implementation: 'dbt singular data test: assert_order_line_revenue_conservation'
    dimension: consistency
    description: Bespoke table-level dbt assertion.
customProperties:
- property: dpf.survivorship.entity
  value: order
- property: dpf.survivorship.sourcePriority
  value:
  - CARTIVO
  - MERCARO
- property: dpf.survivorship.rules
  value:
    source_record_id: first_non_null
    source_order_id: first_non_null
    source_customer_id: first_non_null
    order_date: most_recent
    order_status: most_recent
    currency: first_non_null
    order_total: most_recent
    channel: first_non_null
